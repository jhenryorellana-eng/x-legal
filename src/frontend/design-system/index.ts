/**
 * Design system entry (DOC-01 §9).
 * CSS tokens/motion/shadcn-theme are imported from `globals.css`; this barrel
 * re-exports the JS surface (fonts) for convenience.
 */
export { plusJakartaSans } from "./fonts";
