# PROGRESO — UsaLatinoPrime V2

> Archivo de continuidad entre sesiones (PROMPT-CONSTRUCCION-V2 §4). Actualizar al cierre de cada sesión.
> Biblioteca SoT: `C:\Users\mauri\Documents\Trabajos\USALATINO V2\V2\docs\` · Supabase: **USALATINO V2** `uexxyokexcamyjcknxua`

> **⚠️ CONTINUIDAD ACTUAL (2026-06-28):** la memoria viva del proyecto es `docs/historial/` (no este archivo, que quedó en la era F0–F8). Para retomar el trabajo en curso lee:
> `docs/historial/2026-06-28-handoff-etapa-c-d.md` (handoff detallado: **Etapa B `ai_field` ✅ en prod** · **Etapa C backend en rama `feat/staff-prior-phase-docs`, UI pendiente** · **Etapa D RAG+Pre-Mortem sin empezar**).

**Fase actual: F8 — cierre V2.0 ✅ (código completo; go-live = acciones externas de Henry). Olas: `8a shell móvil` ✅ · `8b PWA Serwist + offline + push` ✅ · `8c bilingüe ES/EN` ✅ · `8d platform-bridge / Capacitor-ready` ✅ · `8e seguridad CSP+RLS 30/30+migración` ✅ · `8f perf+a11y` ✅ · `8g go-live prep` ✅ (runbook `GO-LIVE.md`).** + **FIX tipografía** (Plus Jakarta Sans, ver abajo). **F8 código-completo: queda el evento de go-live (deploy/DNS/cuentas/seeds/schedules) guiado por `GO-LIVE.md`.**

### 2026-06-24 — "IA extrae": extracción de documentos (Gemini) → autocompletado de formularios con badge "IA" ✅ — 5 gates verdes + verificado en vivo E2E (Playwright) + Gemini real confirmado — sin migración
Henry pidió implementar de punta a punta la opción **"IA extrae"** del editor de servicios: el admin define qué campos extrae la IA de cada documento; en **Formularios** conecta cada campo del formulario con un dato concreto de un documento; cuando el cliente sube el documento, **Gemini extrae** automáticamente, se le muestra lo leído para **continuar**, y en el formulario el campo aparece autocompletado con un **badge "IA"**. Lo extraído se guarda en BD.
- **Hallazgo clave**: el modelo de datos y casi todo el backend **ya existían** (toggle `ai_extract`, `extraction_schema`, tabla `document_extractions`, job `extract-document` auto-encolado al subir, `resolveBySource` para `source='document_extraction'`, validaciones, `proposeExtractionSchema`, picker doc↔campo `DocExtractionPicker`). El CHECK de `form_questions.source` ya permitía `'document_extraction'`. **Cero migraciones** — todo fue cableado de UI + fixes de robustez.
- **Admin — modal "Esquema…"** (nuevo `extraction-schema-modal.tsx` + DocsStep del `catalog-wizard`): junto al switch "IA extrae", botón **"Esquema…"** abre un modal de 2 paneles (editor JSON con validación en vivo + vista de campos derivada) con **"Proponer con IA"** (usa `proposeExtractionSchema`, Sonnet). `WizardDoc.extraction_schema` + persistencia en create/update + loader (`[serviceId]/page.tsx`). Nueva `validateExtractionSchemaAction` (única fuente de verdad = `validateExtractionSchema` de domain).
- **Formularios (tu idea)**: el origen `document_extraction` ya listaba el picker; ahora el dropdown de **ruta (`json_path`)** se llena con los campos del `extraction_schema` (`getServiceSlugIndex`→`schemaPaths`). El admin decide la ubicación exacta → la IA no adivina dónde colocar el dato (un documento puede llenar varios campos).
- **Cliente — subida**: `upload-screen.tsx` ahora, tras subir un doc con `ai_extract`, muestra **"Analizando tu documento con IA…"** (polling a nueva `getDocumentExtractionStatus`/`getExtractionStatusAction`) → **pantalla de revisión de solo lectura** ("Esto leímos de tu documento") → **Continuar** → celebración. Si falla/expira, mensaje amable (no bloquea). `confirmUploadAction` ahora devuelve `caseDocumentId`; `DocumentMatrixItem` expone `aiExtract`.
- **Badge "IA"** (`form-wizard/fields.tsx` `PrefillChip`): un campo `document_extraction` muestra un badge **"IA"** explícito + "lo tomamos de tu documento". Acotado a esa fuente (no a `profile`). Label `prefillAiBadge` en los 4 builders de `WizardLabels`.
- **Decisiones de producto (Henry)**: (1) el prefill aparece **apenas termina la extracción** (doc aún `uploaded`, sin esperar aprobación del staff) → se relajó `findApprovedDocumentBySlug`→`findLatestActiveDocumentBySlug` (status in uploaded/approved); (2) revisión **solo lectura** + Continuar (las correcciones se hacen luego en el formulario); (3) editor de schema = "Proponer con IA + JSON editable" (spec PROMPT-ADM-04 §Paso 4).
- **Fix de robustez**: `executeExtractionJob` construía mal el schema de Gemini (metía el JSON Schema completo dentro de `properties`) → reescrito para fusionar `properties` + inyectar `raw_text` y degradar si es null.
- **Gates**: tsc **0** · eslint **0** · check:i18n **OK (paridad es/en)** · vitest **1421** · **build 0** (se reparó `@parcel/watcher` que tenía el `index.js` faltante por caché npm corrupta — `npm cache clean --force` + reinstalación; no afecta `package.json`).
- **Verificado en vivo (Playwright `:3100`, `AI_E2E_STUB=1`)**: admin definió el esquema de Pasaporte y Cédula (Proponer IA→`full_name/document_number/issue_date`→Guardar→**persistido en BD**); en el editor de I-589 mapeó un campo a `pasaporte.full_name` (el dropdown de ruta se llenó con los 3 campos); **badge "IA"** visible en la previsualización del wizard; cliente subió la Cédula → **"Analizando con IA…"** → (job disparado vía seam E2E `x-e2e-qstash-bypass`) → **revisión** con el dato → Continuar → celebración; `document_extractions` quedó `completed` con `payload` + tokens.
- **STUB vs REAL (aclaración para Henry)**: en la prueba en vivo salió `full_name="stub-full_name"` **porque el dev corría en modo stub** (`AI_E2E_STUB=1`, sin llamar a Gemini, para no gastar tokens). Con **Gemini real** (script que replica la llamada exacta sobre `i-589-LLENADO.pdf`) extrajo el **nombre real**: `full_name="Carlos Ramírez"`. En **producción el stub NO existe** (el código lanza error si se intenta activar en prod) → siempre se usa Gemini real → el cliente verá su nombre real. El I-589 es un formulario, por eso `document_number/issue_date` salieron `null`; con un pasaporte/cédula reales esos campos también se llenan según el esquema definido por el admin.

### 2026-06-20 — FIX build de prod (NO era serwist) — root cause = `@swc/helpers` lockfile drift ✅ — `npm ci` + build verdes
La "deuda de serwist" documentada antes era un **diagnóstico incorrecto**. Producción (Vercel/Linux) siempre compiló bien (sw.js de 50KB live). El build local (Windows) y **CI** fallaban por otra cosa:
- **CI fallaba en `npm ci`** (no en el build): `npm error Missing: @swc/helpers@0.5.23 from lock file`. Causa: `@swc/core@1.15.41` (vía `next-intl@4.13.0`) tiene **peerDependency `@swc/helpers>=0.5.17`**, pero `next@15.5.19` fija exacto `0.5.15`. El lock committeado quedó inconsistente (npm 11 local lo toleraba; npm 10 de CI lo rechaza). Borrar node_modules+lock no bastó (npm 11 re-dedupea 0.5.15).
- **El build local de serwist** fallaba con `webpack-runtime "Cannot read properties of undefined (reading 'call')"` en `/_error` — **mismo root cause**: los helpers de SWC se inyectan en cada módulo transpilado; el `@swc/helpers@0.5.15` inválido rompía el runtime. Serwist solo lo *exponía* (su pass extra de webpack tocaba `/_error`).
- **Fix**: `overrides: { "@swc/helpers": "0.5.23" }` en `package.json` (satisface a ambos; los helpers 0.5.x son API-compatibles) + lock regenerado. Verificado: `npm ci` **exit 0** · `npm run build` **exit 0 (30/30 páginas, serwist HABILITADO)**.
- **+ Fix time-bomb test** (`scheduling/__tests__/reschedule-atomic.test.ts`): usaba `OLD_STARTS="2026-06-20T14:00:00Z"` hardcodeado → empezó a fallar al cruzar las 14:00 UTC de hoy (guard `APPT_ALREADY_STARTED`). Ahora usa fechas relativas a `now`.
- **Gates finales**: tsc 0 · eslint 0 · check:i18n OK · vitest **1337** · **build 0**. La deuda de serwist queda **cerrada**.

### 2026-06-20 — Partes/roles del caso por servicio (admin) + modal constreñido (Vanessa) ✅ — gates verdes + verificado en vivo + migración 0023 aplicada a prod
**Problema (visto en vivo)**: el modal "Nuevo caso" pedía el rol de cada parte como **texto libre**, pero `case_parties.party_role` tiene un CHECK con 8 valores fijos → cualquier texto ("Solicitante principal") era rechazado por la BD → **bloqueaba crear casos**. Henry pidió resolverlo de raíz: el **admin define por servicio** qué partes/roles tiene un caso (además del solicitante), con cardinalidad; **Vanessa solo elige esos roles**; los documentos se asignan a partes/miembros.
- **Migración 0023** (`supabase/migrations/0023_service_party_roles.sql`, **aplicada a prod 2026-06-20 con autorización de Henry** vía Management API `docs/_evidence/apply-0023.cjs`): nueva tabla `service_party_roles(service_id, role_key∈8keys, label_i18n, cardinality single|multiple, is_required, position)` + trigger + **RLS** espejando `service_phases`. Sin tocar el CHECK de `case_parties`. `database.types.ts` actualizado a mano.
- **Fuente única**: `src/shared/constants/party-roles.ts` (8 keys + labels es/en + `PRINCIPAL_ROLE_KEY='petitioner'` + `isPartyRoleKey`). Alineado el enum obsoleto `PartyRole` de `cases/domain.ts`.
- **Catálogo (admin)**: `catalog/{domain,repository,service,actions,index}` con `ServicePartyRole` + CRUD (`listServicePartyRoles`/`create`/`update`/`delete`, con **chequeo de org** tras el code-review) + `getServiceEditorTree` incluye `partyRoles` + validación `party_roles⊆keys` en documentos. Wizard: nuevo paso **"Partes"** (STEP_IDS 6→7) para definir roles (tipo+etiqueta+cardinalidad+requerido); el editor de documentos pasó de CSV libre a **multiselect** de `{Solicitante}∪roles`.
- **Alta (Vanessa)**: `NewCaseService.partyRoles` (armado en `ventas/leads/page.tsx` y `admin/casos/page.tsx`). Modal paso 2: **solicitante auto-incluido (read-only)** + inputs por rol respetando cardinalidad (único=1, múltiple=repetible), sin texto libre. `createCaseFromContract` **auto-agrega el solicitante** (role `petitioner`, position 0) y **valida** los roles adicionales ⊆ roles del servicio (`CASE_PARTY_ROLE_INVALID`); posiciones 1..N.
- **Cliente (Parte C)**: la visualización per-parte ("ID de [nombre de cada parte]") **ya funcionaba** (`expandPerPartyRequirements` + `case_documents.party_id` + `resolvePartyName`); sin cambios.
- **Code-review (REGLA #3)**: 1 CRITICAL + 4 IMPORTANT corregidos (chequeo de org en create/update/delete de party-role; error retryable en vez de "rol inválido" si falla el catálogo; filtro `org_id` en el fallback de servicio; toast correcto `docNeedRoles`).
- **Verificado en vivo (Playwright `:3100`)**: admin (Henry) definió en Asilo **Cónyuge (único)** + **Hijos (múltiple)** → persistido en `service_party_roles`. Vanessa creó un caso de Asilo: el modal mostró "Solicitante (automático): Carlos Mendoza" + Cónyuge + Hijos (chip Múltiple, sin texto libre); agregó 1 cónyuge + 2 hijos → **caso creado sin error de constraint**. `case_parties`: petitioner(pos0, user) · spouse "Lucía Ramírez"(pos1) · minor "Mateo"(pos2) · minor "Sofía"(pos3).
- **Gates**: tsc **0** · eslint **0** · check:i18n **OK (1683 claves)** · vitest **1337 verde** (tests nuevos: solicitante auto pos0, posiciones 1..N, rechazo de rol no declarado/principal duplicado). Build = **deuda pre-existente de `@serwist/next`** (ajena a esta feature).

### 2026-06-20 — Login del cliente SOLO por teléfono (retira email-OTP) + alta con dirección/teléfono/correo obligatorios ✅ — gates verdes + code-review
Decisión de Henry (TEMPORAL, luego será teléfono+SMS-OTP): el cliente entra con **solo su número de teléfono, sin código**. Se **retiró** el login email-OTP.
- **Mecanismo**: contraseña determinística derivada en backend (`HMAC-SHA256(phoneE164, SUPABASE_SERVICE_ROLE_KEY)`, `derivePhonePassword` en `identity/domain.ts`) + `signInWithPassword({email, password})` sobre el SSR client (mismo patrón probado que `signInStaffAction`). El cliente teclea solo el teléfono; el backend lo resuelve (`findClientByPhone`→email) y firma. **Sin OTP, sin SMS, sin secreto nuevo**. `provisionClientUser` setea la password; login legacy se auto-cura (`updateUserById`+retry).
- **Backend** (`identity/{domain,repository,service,actions,index}`): nuevo `loginClientByPhone` + `loginClientByPhoneAction`; eliminados `requestClientOtp`/`verifyClientOtp` y sus actions + los gates de email. Reutiliza `checkClientEligibility` (por teléfono, ya existía) + re-gate por id. Anti-enumeración: no-encontrado/no-elegible/sign-in-fail → mismo `wrong_kind`/"no_access" + piso ~800ms.
- **Frontend**: renombrada `(cliente)/email`→`(cliente)/entrar` (un input de teléfono → `/home` directo); **eliminada** `(cliente)/otp`; `/welcome` link `/email`→`/entrar`; middleware allowlist `/email`,`/otp`→`/entrar`. i18n reusa `cliente.phone`.
- **Alta de caso**: el modal exige **dirección US completa** (calle, apto, ciudad, estado, CP) + **teléfono** (credencial de login) + correo (identidad interna). Dirección → `client_profiles.address` (JSONB) → pre-llena I-589 (`"address.apartment"` agregado a `PROFILE_SOURCE_FIELDS`).
- **Code-review**: fixes aplicados (tipos `PhoneLoginResult`, códigos de error muertos removidos, `isValidPhone` ajustado, comentarios/mocks de test). **Migración/backfill**: clientes sin teléfono no pueden entrar (pocos tras el reset) — backfill manual si se necesita.
- **Verificado**: `/entrar` renderiza (input teléfono); `/email` y `/otp` → 404. **Riesgo aceptado por Henry**: teléfono sin 2FA (temporal hasta SMS-OTP).
- **⚠ DEUDA DE TOOLING — build de prod roto por `@serwist/next`** (NO por esta feature; decisión de Henry: dejarlo como deuda): `next build` falla en el SSG (`Cannot read properties of undefined (reading 'call')`, página variable /login↔/email). **Causa raíz confirmada**: `@serwist/next@9.5.11` (PWA SW) deja `build-manifest.json` sin los archivos de `/_error` → revienta el prerender. Probado desactivando serwist → **build pasa 31/31**. Serwist **ya está en su última versión** (no hay upgrade); incompatibilidad con **Next 15.5.19**. Pre-existente y a nivel de rama (HEAD tampoco compila). Verificación del proyecto es vía `next dev` (`next start` ya tenía issues conocidos). **Pendiente como tarea de tooling separada**: bajar Next a una 15.x compatible, o workaround/patch de serwist que mantenga el SW.

### 2026-06-19 — I-589 A.III: split de campos colapsados + grounding del propose con texto del PDF ✅ — gates verdes
Dos cosas, a raíz de que Henry notó que "¿Cuál ha sido su empleo más reciente?…" era UN textarea pero cada dato va a un campo distinto:
- **Fix del A.III (borrador v2)**: el propose había **colapsado Y mismapeado** la sección — "educación"→`TextField13[8]` (en realidad `residence_1_street`), "empleo"→`[16]` (`residence_3_street`), "direcciones"→`TextField35[0]` (`mother_current_location`). Usando el **field-map autoritativo de v1** (`repo/.../i589-official/field-map.ts`), borré las 4 preguntas colapsadas y creé las preguntas **separadas correctas**: empleo (empleador/ocupación = text, desde/hasta = date → `TextField13[39][40]`,`DateTimeField42/43`), educación (escuela/tipo/ubicación + 2 fechas), residencias 5-años (calle/ciudad/estado/país + 2 fechas), última dirección. **Auditoría**: 60 preguntas, **0 sin mapear, 0 referencias inválidas, 0 duplicados**, tipos correctos. Autocompletado verificado: el "Generar PDF de prueba" llenó sin error (fill por nombre de campo → posición verificada del map de v1).
- **Mejora del propose (para futuros servicios, pedido de Henry "0 a 100")**: la causa raíz de los mismapeos es que la IA recibía nombres **anónimos** (`TextField13[39]`) sin contexto. Ahora `aiProposeStructure` **extrae el texto impreso del PDF** (`extractPdfText` nuevo en `platform/pdf.ts`, vía mupdf `toStructuredText().asText()`, por página) y se lo pasa al propose; el prompt instruye a **mapear por SIGNIFICADO** usando las etiquetas + la página (p. ej. "tabla de Empleo en pág. 4 → TextField13[39]=Empleador"), nunca por el nombre anónimo. Verificado: la extracción del I-589 pág. 4 devuelve las etiquetas reales ("Name and Address of Employer", "Your Occupation", "City/Town", …).
- **Gates**: tsc 0 · vitest **1326** · eslint src 0 · build 0 · i18n OK.

### 2026-06-20 — Re-verificación empírica de A.I / A.II del I-589 — corrige mismapeos del propose original ✅
La DOB salía "N/A" porque el propose ORIGINAL había mismapeado varios campos de A.I/A.II (igual que A.III antes de remapearla). Descubrimiento empírico de las págs. 1-3 (`discover-text123.mjs` + `discover-checks123.mjs` → `text-p1/p2.png`, `text123.txt`, `checks123.txt`) → mapa autoritativo por coordenadas. **Fixes** (`fix-ai-aii.cjs`, datos sobre la v2 publicada, 0 duplicados):
- **A.I (6 campos)**: A-Number `InCareOf`→`PtAILine1_ANumber[0]` · **DOB `DateTimeField6`→`DateTimeField1[0]`** · país nac. `TextField3`→`TextField1[4]` (l.13) · nacionalidad `TextField4[0]`→`TextField1[3]` (l.14) · raza `TextField5[0]`→`TextField1[6]` (l.16) · religión `TextField5[1]`→`TextField1[7]` (l.17).
- **A.II cónyuge**: DOB `DateTimeField8` (l.9 "Date of Marriage")→`DateTimeField7[0]` (l.3 DOB) · select "¿cónyuge en EE.UU.?" de l.22 (proceso judicial)→`PtAIILine15_CheckBox15` (l.15). Los campos de hijos (`ChildLast/DOB/Nat`) ya estaban bien (nombres semánticos).
- **Verificado**: re-generé el PDF de Rosa (staff "Regenerar PDF" → `generateFilledPdf` real) → pág. 1 ahora muestra **DOB 1985-06-15** (antes N/A), lugar de nac./nacionalidad/raza/religión en su línea correcta. `rosa-p1.png`. **I-589 Parte A correcto de punta a punta.**

### 2026-06-20 — E2E flujo de CLIENTE real (Playwright) sobre Asilo + I-589 Parte A ✅
A pedido de Henry ("ingresar como cliente y completar el formulario").
- **Setup** (`mint-asilo-client.cjs`): no había clientes ni casos (reset). Creé cliente **Rosa** (auth + `public.users kind=client`; el `custom_access_token_hook` puebla `org_id`/`user_kind`), un **caso de Asilo** (fase-1, plan self) + `case_members(owner)`, y la **cookie SSR** (base64url). Cookie inyectada vía `browser_run_code_unsafe` (filename con el valor embebido; el runner no tiene `require`/`import`).
- **Wizard real del cliente** (`/caso/{id}/formulario/{i589}`, mismo `FormWizard`): llené los 4 pasos con datos inventados (Rosa Elena Pérez, soltera, sin hijos). Verificado el funcionamiento de los **condicionales**: A.II cónyuge/hijos colapsaron al responder "No"; en **A.III** las preguntas-conteo (residencias=1, educación=1, empleo=1) **revelaron las filas condicionales** que luego llené. Sexo→Femenino, Estado civil→Soltera.
- **Envío**: "Terminar" → **status `submitted`** · `translation_status='done'` (traducción es→en automática vía Gemini) · **51 respuestas** persistidas (autosave) · navegó a `/exito`. Captura `client-exito.png`.
- **Fix i18n** (`exito/page.tsx`): la pantalla de éxito mostraba la **clave cruda `cliente.exito.title`** — causa: el mensaje `"¡Lo lograste, {name}!"` tiene placeholder ICU `{name}` y el código llamaba `t("title")` SIN pasar `name` → next-intl falla y devuelve la ruta como fallback (el `body`, sin placeholder, sí resolvía). Fix: `t("title", { name: displayName })`. Verificado en vivo → "¡Lo lograste, Rosa!". (+ creé `client_profiles` de Rosa para que el nombre resuelva). Gates: tsc 0 · check:i18n OK · eslint 0.
- **Aprobación staff + PDF de producción** (Playwright como Henry): `/admin/casos/{id}/formularios` → "Aprobar" (submitted→approved) → "Generar PDF" → `generateFilledPdf` real escribió `filled_pdf_path` en el bucket `generated`. Descargado + renderizado (`download-filled.mjs` → `rosa-p1..p4.png`):
  - **Pág. 4 (A.III) impecable**: las respuestas reales de Rosa en su celda correcta; overflow oculto (res/edu/emp=1, sib=0) → **N/A**; familia OK; traducción es→en (Administradora→Administratrix, Distrito Capital→Capital District).
  - **Pág. 1 (A.I)**: nombre/dirección OK; **Sexo→[X]Female, Estado civil→[X]Single, Juez→a** (los fixes select→checkbox vivos en prod); **N/A en todos los vacíos**.
  - **Hallazgo**: la **DOB sale "N/A"** — la fecha cayó en otro box (mismapeo pre-existente del propose de A.I). Es el mismo tipo de error de campo de A.I/A.II que ya estaba flagueado (re-verificación empírica de A.I pendiente). El N/A evita el rechazo por blanco, pero el dato está en el box equivocado.

### 2026-06-19 — Selects → checkbox correcto (Sexo/Estado civil/Sí-No) — mapeo opción→campo ✅ — gates verdes
Segundo ítem obligatorio de aceptación (los *select* marcaban siempre la opción [0]).
- **Modelo** (`catalog/domain.ts`): cada opción de un `select` puede llevar su propio `pdf_field_name` (grupo de checkboxes). **Fill** (`cases/generateFilledPdf` + `catalog/generateTestPdf`): si las opciones traen `pdf_field_name`, marca la **casilla de la opción elegida** (no la [0]); retrocompatible (selects sin option-fields siguen igual). +`options` añadido a los tipos del loop.
- **Datos** (`rebuild-selects.cjs` + fix juez): mapeo **empírico** (dump `checks123.txt` + render) de las 9 selects de A.I/A.II → Sexo (male/female→`Sex[0/1]`), Estado civil (→`Marital[0..3]`), juez (no/ongoing/completed→`CheckBox3[0/2/1]` — b/c estaban invertidos, corregido), casado (`CheckBox5[0]`), cónyuge/hijo Sí/No (→`_Yes/_No[0]`), tiene-hijos (`ChildrenCheckbox[1/0]`).
- **Verificado** (`verify-selects.mjs` → `selects-p1.png`): con respuestas **no-default** (Femenino/Divorciado/juez-c), el render marca la **casilla correcta** ✓.
- **Code-review** (REGLA #3, ambos cambios N/A + selects): **APPROVED**, 0 blockers. Fixes aplicados: (1) el page-scope del N/A-backfill ahora siembra también los `pdf_field_name` por-opción (una página de solo selects ya no se subcuenta); (2) regex office-use ampliado (`received|remarks|action block|for eoir`); (3) comentario "see below" huérfano. +5 tests del helper.
- **Hallazgo (pre-existente del propose A.II)**: las preguntas "¿cónyuge/hijo en EE.UU.?" apuntan a la **línea 22/20** (proceso judicial / estatus) en vez de la **línea 15/13** ("en EE.UU."). Es un mismapeo de campo del A.II original (no del mecanismo). Marca una casilla (no queda en blanco) pero la línea es incorrecta → pendiente re-verificar A.II empíricamente (los grupos cónyuge/hijos son condicionales).
- **Gates**: tsc 0 · eslint 0 · vitest **1331** · build 0.

### 2026-06-19 — Aceptación USCIS: relleno "N/A" de campos vacíos (8 CFR 1208.3(c)(3)) ✅ — gates verdes
Henry pidió "solo los campos obligatorios para que lo aprueben; investiga y corrobora; testéalo". **Investigación** (uscis.gov + 8 CFR §1208.3(c)(3) + cliniclegal): el I-589 se **rechaza si CUALQUIER campo queda en blanco**, pero las instrucciones permiten responder **"N/A"/"None"** en los no aplicables. Es decir, lo obligatorio para aceptación = **ningún recuadro vacío**.
- **Hallazgo del test** (`fulltest.mjs`): A.III quedó completo, pero A.I/A.II (págs. 1-3) tenían **166 campos del solicitante sin pregunta** → se llenarían en blanco (los dejó el propose original incompleto).
- **Fix general** (`platform/pdf.ts` `backfillNaTextFields`): tras mapear las respuestas, el motor de llenado escribe **"N/A" en todo campo de TEXTO vacío de las páginas que cubre el formulario** (page-scoped, para no pisar otras partes en formularios divididos; nunca toca checkboxes ni campos office-use/firma). Cableado en **ambos** motores reales: `cases/generateFilledPdf` y `catalog/generateTestPdf`. +5 tests unitarios del helper.
- **Test** (`fulltest2.mjs` → `accept-p1..p4.png`): 135 mapeados + **120 "N/A"** = **0 recuadros de texto en blanco** en págs. 1-4. Verificado visualmente (pág. 1: SSN/cuenta USCIS/apt/historial de entradas → "N/A").
- **Gates**: tsc 0 · eslint 0 · vitest **1331** · build 0.
- **PENDIENTE para aceptación plena**: los *select* mapean a un solo checkbox (Sexo siempre marca "Male"; Estado civil "Single"; el lado "No" de los Sí/No no se marca). Necesita mapeo opción→campo (modelo + fill). Es el siguiente ítem obligatorio.

### 2026-06-19 — A.III reconstruido desde mapa EMPÍRICO (corrige mismapeos) + #3 familia/fallecido + overflow + IA con geometría ✅ — gates verdes
Henry pidió completar #3 (padres/hermanos fallecidos) + filas de overflow y, sobre todo, **que la IA no vuelva a fallar**. Al verificar con rigor apareció un **bug grave heredado**: la `fix-a3-split.cjs` confió en los índices *estimados* del field-map de v1 (que tiene colisiones documentadas, p.ej. `education_1` vs `residence_5` comparten `TextField13[24]`) → varios campos de A.III estaban **mismapeados** (empleo, educación, fechas de residencia, e incluso los nombres de madre/padre apuntaban a celdas de hermanos).
- **Descubrimiento empírico** (`discover-page4.mjs` → `discover-text.png` + `discover-widgets.txt`): rotulé cada uno de los 99 widgets de la pág. 4 con su nombre y rendericé → **mapa autoritativo por coordenadas** (fila = misma `y`, columna = misma `x`). Reveló los índices reales (empleo fila1 = `T13[40]`/`T13[42]`/`DTF42`/`DTF44`; educación = `T13[28/30/32]`+`DTF41/40`; familia: nombres `T13[46/47]`, hermanos `T13[48/52/54/56]`, fallecido = checkboxes `CheckBoxAIII5\.{m,f,s1..s4}[0]`, ubicación `TextField35[0..5]`).
- **Reconstrucción completa** (`rebuild-a3-empirical.cjs`): borré y recreé A.III → **103 preguntas** (28 fechas, 6 checkboxes de fallecido, 4 conteos, 84 condicionales, 5 compuertas sin-pdf). Cobertura total: Sección 1 (2 filas), residencias (5), educación (4), empleo (3), padres + 4 hermanos (nombre / ciudad-país nac. / **fallecido** / ubicación). **Overflow** = filas 2+ condicionadas por preguntas-conteo (`op gte`, patrón "Hijo N"). **0 duplicados** de pdf_field_name.
- **Verificación de posición** (`verify-render.mjs` → `verify-page4.png`): llené los **98 campos pdf-mapeados** con su etiqueta y rendericé → **cada celda correcta** (Res1-5, Edu1-4, Emp1-3, Madre/Padre/Herm1-4 con su checkbox Deceased marcado). 0 widgets faltantes.
- **IA robusta — fix de raíz** (para que no se repita): la causa era que la IA recibía nombres anónimos sin geometría y adivinaba por índice. `proposeFormSegmentation` ahora recibe el **`rect` (coordenadas)** de cada campo; el prompt incluye regla **"FIELD GEOMETRY — reconstruct tables spatially"** (filas=misma y, columnas=misma x, "NUNCA asumas que el índice `[8],[9]` sigue el orden visual") + **"ACCOUNT FOR EVERY FIELD"**; el listado de campos va **ordenado en orden de lectura** con `@x,y`; STEP A (research) pide nº de filas/slots + orden de columnas + obligatoriedad. `aiProposeStructure` pasa `rect` y registra un **chequeo de cobertura determinístico** (campos detectados sin mapear). Archivos: `ai-engine/service.ts`, `catalog/service.ts`.
- **Gates**: tsc 0 · eslint 0 · vitest **1326** · build 0 · check:i18n OK (1671).
- **Code-review** (REGLA #3): `code-reviewer` → **APPROVED** (0 blockers). Fixes aplicados: timeout explícito `300_000` en `messages.stream` (alinea con `maxDuration=300`); `pdfText.slice` 16k→32k + nota de marcadores `=== Page N ===` por página; comentario stale "20-45"→"~40-75"; prefijo de log de cobertura `ai-engine:`→`catalog:`; comentario que documenta por qué la rama UUID-directo de `resolveProposedCondition` es segura (solo ids recién minteados de esta versión). Re-gates: tsc 0 · eslint 0 · vitest 276 (ai-engine+catalog).
- **PUBLICADO**: v2 → `status=published` (v1 archivada) vía la UI admin (acknowledge de campos no-Parte-A). El I-589 Parte A corregido y completo está **en vivo**.

### 2026-06-19 — Verificación del autocompletado A.III con datos inventados (render PDF + wizard real) ✅
A pedido de Henry ("usa datos inventados", corroborar posición). Dos pruebas independientes sobre el **borrador v2**:
- **Render del PDF llenado** (`docs/_evidence/a3-test/{fill-and-render.mjs,filled.pdf,page-4-a3.png}`): replica fiel de `fillAcroForm` (mupdf, mismo motor de prod) con datos inventados distintos y memorables; los **60 widgets** se llenan y los **23 campos de A.III caen en la página física 4**. El render confirma **cada respuesta en su celda propia**: empleo (empleador/ocupación/desde/hasta en 4 celdas), educación (nombre/ubicación/fechas), residencia 5-años y última dirección (calle/ciudad/provincia/país/desde/hasta), madre y padre. **Posición verificada.**
- **Wizard real del cliente** (Previsualizar del editor = mismo `FormWizard`): recorrido completo A.I→A.II(cónyuge)→A.II(hijos)→**A.III**, con condicionales colapsando cónyuge/hijos al responder "No". **Paso 4 de 4 = Parte A.III con 23 preguntas separadas**; el empleo aparece como 4 preguntas distintas y se respondieron con datos inventados (`docs/_evidence/a3-test/wizard-employment.png`).
- **Hallazgo (para #3 pendiente)**: el render reveló que la tabla de familia del I-589 **tiene columna "Deceased"** (checkbox Madre/Padre + 2 filas de Hermanos) + "Current Location" — hoy solo mapeados los **nombres** de madre/padre. También residencia/educación/empleo tienen **filas adicionales** (solo fila 1 mapeada = slots de overflow). **v2 NO publicado aún** (sigue como borrador editable) — pendiente mapear fallecido/ubicación/hermanos + filas extra antes de publicar.
- **Helper nuevo**: `docs/_evidence/q.cjs` (runner SQL genérico vía Management API).

### 2026-06-19 — Visor PDF: nitidez + alineación de overlays (reporte de Henry "no se entiende / no está alineado") ✅ VERIFICADO EN VIVO
Tres problemas del visor del editor (`pdf-viewer.tsx`), todos arreglados:
- **Nitidez (race condition)**: el render pintaba en un `queueMicrotask` ANTES de que el `<canvas>` se montara (`canvasRefs` null) → el backing quedaba en 300px default y se escalaba a 826px = **borroso**. Fix: separar carga (mide dims) de pintado (effect que corre tras montar los canvases, `[pages]`) + render a **devicePixelRatio** → `backingW:826 = cssW:826` (nítido; 2× en retina). **Verificado**: se lee "USCIS Form I-589", "Part A.I"…
- **Alineación (otra forma)**: los overlays se posicionaban con las coordenadas de **mupdf** (detección) pero el render es de **pdfjs** → sistemas de coordenadas distintos → cajas desalineadas. Fix: **usar las anotaciones de pdfjs** (`page.getAnnotations()` + `viewport.convertToViewportRectangle`) — mismo origen que el render → **alineación garantizada**. Los `fieldName` de pdfjs coinciden con los `pdf_field_name` de mupdf (mismo path XFA), así que el estado mapeado/sin-mapear + el click-to-map (D2) siguen funcionando. **Verificado en vivo**: 460 overlays · 43 mapeados, cajas sobre las celdas reales del formulario.
- **Legibilidad**: overlays sutiles (sin-mapear = solo contorno punteado → se lee el formulario debajo) + botón **"Ver PDF oficial"** (abre la URL firmada en el visor nativo del navegador, full zoom/lectura).
- **Gates**: tsc 0 · vitest 1326 · eslint src 0 · build 0 · i18n OK.

### 2026-06-19 — Mejoras al editor de formularios + propose IA (D1-D4) ✅ — gates verdes + VERIFICADO EN VIVO
4 mejoras pedidas por Henry al editor pdf_automation + la calidad del propose:
- **D1 — Visor PDF (bug cross-bundler)**: el worker de pdfjs se cargaba con `import("...pdf.worker.min.mjs?url")` — el sufijo `?url` solo lo resuelve **Turbopack**; bajo **Webpack** (`npx next dev` Y el `next build` de prod) el worker quedaba sin setear → el canvas nunca pintaba (panel derecho "vacío"). Fix: `new URL("pdfjs-dist/build/pdf.worker.min.mjs", import.meta.url)` (lo honran ambos bundlers). **Verificado**: las 12 páginas del I-589 renderizan (`canvasCount:12`).
- **D2 — Click-to-map**: nuevo en `structure-editor.tsx handleSelectField` — si hay una pregunta expandida (y no read-only), **hacer clic en un campo del PDF lo asigna a esa pregunta** (`patchQuestion`). Si no, salta a la pregunta ya mapeada (comportamiento previo). Hint en el card + toast. **Verificado en vivo**: clic en un overlay → el dropdown de la pregunta cambió al campo clicado. (La creación manual de preguntas + dropdown ya existían.)
- **D3 — Editabilidad de versión publicada**: `duplicateVersionAsDraft` (catalog/service) crea un **borrador editable copiando** source_pdf + detected_fields + grupos + preguntas, con **remapeo de condiciones old-id→new-id** (2 pasos, igual que el materializador). Botón "Crear versión editable" en el banner de inmutabilidad (pdf-mode) → navega a `?v=<draft>`. La publicada queda intacta. **Verificado en vivo**: v2 borrador con 43 preguntas + 18 condiciones copiadas, refs resuelven en el borrador (`ok:true`).
- **D4 — Mejorar el propose IA (para próximos servicios)**: prompt de `proposeFormSegmentation` reforzado — research más profundo (5 búsquedas, ~650 palabras, extrae estructura de subcampos + familia); reglas nuevas: **NO colapsar secciones estructuradas** (direcciones/educación/empleo) en un textarea → una pregunta por subcampo (calle/ciudad/estado/desde-hasta) mapeada a su campo distinto; **mapear cada campo AcroForm distinto** (un campo por caja, no por concepto); **completitud** (no saltar Partes; familia: padres vivos/fallecidos + hermanos). Cobertura subida a ~40-75 (streaming lo permite). Resuelve los hallazgos de Henry (#3 padres/hermanos faltantes, #4 textareas colapsados). El I-589 Parte A actual se puede refinar en el borrador v2 (D3) con el click-to-map (D2).
- **Gates**: tsc 0 · vitest **1326** (+3 del review previo) · eslint src 0 · build 0 (confirma el fix del worker en Webpack/prod) · check:i18n OK.

### 2026-06-19 — Code-review (Olas 1-3) + fixes aplicados (Ola 4.1) ✅ — gates verdes
Two-stage review (REGLA #3). El `code-reviewer` revisó los ~15 archivos de mis cambios (condicionales + propose streaming): veredicto **NEEDS REVISION** (2 MUST-FIX + 6 SHOULD-FIX + 2 nits). **Todos aplicados**:
- **MUST**: (1) `resolveProposedCondition` ahora descarta condiciones **auto-referenciales** (un campo que se condiciona a su propio valor se ocultaría para siempre) — `+ selfId` param; (2) `proposeFormSegmentation` retry: **break en errores no-reintentables** (401/403/400/413, mismo patrón que `executeGenerationJob`) + log de "reintentando" solo cuando queda intento.
- **SHOULD**: `includes` con value `undefined` → `false` (no coacciona a needle `"undefined"`); solo registrar **keys de IA** en keyToId (evita colisión con fallback sintético); comentario en `sameScalar` (coerción bool/string intencional); guard `CATALOG_NO_ACROFORM_FIELDS` en re-propose de grupo sin campos; `import type ProposedQuestion` top-level (en vez de inline `import()`); **try-catch en el pass-2** de condiciones (una falla de persistencia no aborta toda la propuesta).
- **NITS**: `default` de `isConditionSatisfied` → `false` (no `true`); nota de exposición del UUID en la migración 0022.
- **+3 tests** (includes/undefined, self-ref drop, 401 no-retry). **Gates**: tsc 0 · vitest **1326** · eslint src 0 · build 0 · i18n OK.
- **Lo bueno (del review)**: el materializador 2-pasos (insert→resolve) maneja forward-refs correctamente; `parseConditionOrNull` como única puerta Zod para datos no confiables; `deriveFieldState` como single-source-of-truth en 4 callsites (wizard + validación + 2× PDF) elimina la clase de bug "cliente lo muestra, servidor lo ignora"; el cambio a streaming documentado con el porqué.

### 2026-06-19 — "Asilo Político" de punta a punta desde la UI (Ola 3) ✅ — servicio ACTIVO + VERIFICADO EN VIVO
Servicio **Asilo Político** completo y **activo** en el catálogo (construido/completado desde la UI admin + Management API, simulando el flujo del admin).
- **2 fases**: Sustentos (6 docs requeridos: pasaporte/ID con `ai_extract`, I-94, parole/NTA, actas) · Reforzar (7 docs: declaración jurada + evidencias policial/médica/psicológica/amenazas/prensa/testigo).
- **2 planes**: self ($1500/12 cuotas) + with_lawyer ($2500/12 cuotas, requiere validación de abogado).
- **I-589 split (publicado ×2)**: **Parte A** (págs 1-4, Sustentos) = 43 preguntas, 18 condicionales, **publicado** vía el gate de publicación de la UI (ack de campos no-cliente) · **Partes B/C** (págs 5-12, Reforzar) = subido + propose IA (10 grupos siguiendo Part B/C oficiales: bases del reclamo, persecución pasada/futura, CAT, solicitudes previas, terceros países, corte de inmigración) = 30 preguntas, 11 condicionales, **publicado**. La IA distinguió B/C de Parte A **solo por el nombre del formulario** (sin pageRange explícito) — valida el grounding IA-driven.
- **Memorándum de Miedo Creíble** (ai_letter): Opus 4.7 · 17 secciones · web_search · dataset "Precedentes de asilo" (config de sesión previa, intacta).
- **VERIFICADO EN VIVO** (Playwright): publish gate ×2 (ack + publicar) · catálogo admin renderiza "Asilo Político · Migratorio · 2 fases · Self · Con abogado · **Activo**" sin errores 500.
- **PENDIENTE (Ola 4)**: prueba de generación del memorándum con caso de muestra (M1–M11) + el cableado fino **pages 5-12 ← carta** (`generation_output`): hoy las narrativas de B/C son `client_answer` (el cliente describe su persecución, que alimenta el memorándum); el llenado inverso del PDF Part B/C desde la salida estructurada del memorándum depende de definir el output schema del memorándum (se aborda al correr la generación). Gates inalterados desde Ola 2 (Ola 3 = solo datos/UI).

### 2026-06-19 — Propose IA-driven robusto (Ola 2) ✅ — gates verdes + VERIFICADO EN VIVO (I-589: 0→43 preguntas, 14 condicionales)
El propose del I-589 que se "colgaba/0 preguntas" ahora **funciona y genera condicionales**. Refactor de `proposeFormSegmentation` (`ai-engine/service.ts`) + helper `curateInternalFields` (`ai-engine/domain.ts`).
- **Diagnóstico (systematic-debugging con logs reales)** — 3 capas de causa raíz descubiertas en vivo: (1) el `import` dinámico de ai-engine **cacheaba código viejo en dev** → falsos negativos hasta reiniciar el dev server fresco; (2) la salida de ~12-16k tokens tardaba **>120s** y reventaba el `timeout:120_000` con excepción no capturada → fallo silencioso; (3) **el batching empeoraba** (más campos/sección → salida más grande). El fix de subir `max_tokens` chocó con el guard del SDK *"Streaming is required for operations that may take longer than 10 minutes"*. **Causa raíz final: hay que usar `messages.stream().finalMessage()`** (no `create`) para permitir `max_tokens` alto sin truncar.
- **Solución**: **una sola llamada en streaming** (`max_tokens 32000`) con TODOS los campos **curados** como entrada (sin el corte ciego a 180 — el cuello de botella era la salida, no la entrada), salida acotada (**≤40 preguntas, concisa**), y **retry resiliente** (captura excepciones, no solo fallos de parseo). `curateInternalFields` descarta firma/preparer/intérprete/abogado/barcode/page-no (I-589: 460→445). El prompt (de Ola 1) ya pide condicionales Sí/No→explicación + overflow.
- **VERIFICADO EN VIVO** (Playwright + IA real, I-589 Parte A): `result groups:4 questions:43 outLen:31461 stop:end_turn` en ~105s (antes: timeout/0). **4 grupos siguiendo las Partes oficiales** (A.I usted · A.II cónyuge · A.II hijos · A.III antecedentes) · **14 condicionales, todos con `ref_exists:true`** (key→id resuelto end-to-end), incluido el **patrón dinámico de hijos** ("Hijo 1 — …" condicional a "¿Cuántos hijos tiene en total?") y los del cónyuge (visibles solo si casado). Preguntas en español llano siguiendo el formulario. _Nota prod: el propose es síncrono (~150-200s); bajo el límite de 300s de server action de Vercel, pero candidato a job async si crece._
- **Gates**: tsc 0 · eslint src 0 · vitest **1323** (curación + single-call + retry resiliente; se quitaron batch/merge tras revertir el batching) · build ✅ · check:i18n OK.

### 2026-06-19 — Campos dinámicos/condicionales (primitiva genérica de plataforma) ✅ — Ola 1, gates verdes + 0022 APLICADA + VERIFICADO EN VIVO
Sustrato reutilizable por **cualquier** formulario: una pregunta puede **mostrarse / bloquearse-con-mensaje / requerirse** según la respuesta de otra (réplica genérica del `dependsOn` de v1). El overflow (p. ej. 5.º hijo) se modela con la MISMA primitiva (`op:'gte'`) mapeada a los slots de continuación que el propio PDF ya trae — sin hojas Supplement. **Plan**: `~/.claude/plans/quiero-que-hagas-un-cosmic-quill.md`.
- **Helper compartido** `src/shared/form-logic/conditions.ts` (`isConditionSatisfied` + `deriveFieldState`; ops `equals/not_equals/includes/answered/gte/lte`, acciones `show/lock/require`). Vive en `shared` → lo importan wizard frontend Y validación/PDF backend sin violar boundaries.
- **Migración `0022_form_question_conditions.sql`** (additive: `form_questions.condition jsonb`) — **APLICADA a prod 2026-06-19** vía Management API. `database.types.ts` + `QuestionSchema` actualizados.
- **Wizard del cliente** (`form-wizard.tsx`/`fields.tsx`/`build-question-schema.ts`): oculta (show), deshabilita con `<fieldset disabled>` + mensaje (lock) o cambia obligatoriedad (require), reactivo; **servidor = SoT** (no se persiste/llena lo oculto). **Validación servidor** (`validateAnswerTypes`) + **llenado PDF** (`generateFilledPdf` real + `generateTestPdf` admin) saltan los ocultos (PDF en blanco — fiel a v1 "NO ⇒ blank").
- **Editor admin** (`question-card.tsx` ConditionEditor + `structure-editor`/`types`/`build-vm`/`strings`): UI "Mostrar según otra respuesta" (acción + pregunta controladora + operador + valor + mensaje de bloqueo). Persiste vía `upsertQuestion` (condition incluida). Preview fiel: `wizard-bridge` mapea `condition`.
- **IA propose** (`proposeFormSegmentation` + `aiProposeStructure`): emite `key` + `condition` (refiere por key); materializador resuelve **key→id** en 2 pasos (`updateQuestionCondition`); prompt reforzado para Sí/No→explicación + overflow. Drop fail-safe si la key no existe.
- **Gates**: tsc 0 · lint 0 · vitest **1318** (+18: helper, wizard hide/lock, validación, PDF skip, materializador key→id) · build ✅ · check:i18n OK.
- **VERIFICADO EN VIVO** (Playwright, Henry admin, BD real `uexxyokexcamyjcknxua`): editor crea grupo→BD (write path) · lee+renderiza condición seedeada (read path) · edita acción show→lock→BD · **Preview del wizard**: Q1 sin responder ⇒ explicación oculta; "Sí" ⇒ aparece; "No" ⇒ se oculta. Grupo demo limpiado tras verificar (I-589 Parte A queda con 0 preguntas para la Ola 3).
- **PENDIENTE**: Ola 2 (propose IA-driven robusto: lotes por página + overflow) · Ola 3 (Asilo desde la UI) · Ola 4 (prueba de generación + review).

### 2026-06-18 — Wizard Formularios (crear desde el wizard) + Traducción de respuestas (Chrome Translator + Gemini) ✅ — gates verdes + code-review aplicado + 0020 APLICADA
Cierre del gap "paso Formularios" del wizard de catálogo + nueva traducción de respuestas del cliente. **Plan**: `~/.claude/plans/quiero-que-hagas-un-cosmic-quill.md`.
- **Feature 1 — crear formularios desde el wizard** (antes el `FormsStep` era stub que solo listaba): `FormsStep` reescrito (`catalog-wizard.tsx`) con creación (kind pdf_automation/ai_letter, fase, slug auto-derivado, label i18n, filled_by) + listado por fase con estado (Borrador/versión publicada) + activar/desactivar. Wrappers `createFormUi`/`updateFormUi` (`catalogo/actions.ts` + `wizard-actions.ts`); `getServiceEditorTree` enriquecido con `published_version`. **Verificado EN VIVO** (Playwright, Henry admin): creé un formulario que persistió en la BD real (slug `carta-de-prueba-e2e`, luego desactivado — queda inactivo en la BD).
- **Feature 2 — traducción de respuestas** (RNF-036): capacidad `translator` en platform-bridge (`index/web/capacitor.ts`) usando Chrome `Translator`/`LanguageDetector` on-device + **fallback Gemini** server (`translateAnswerText` en ai-engine, que enmascara PII con `maskPii` antes del provider). Idioma origen del PDF: columna `source_language` en `form_automation_versions` + selector en el editor (`upload-stage.tsx`, **verificado en vivo**). Pipeline 3 capas: cliente on-device → server Gemini → on-demand en `generateFilledPdf` (garantiza el PDF en el idioma correcto). Persistencia: `case_form_responses.answers_translated` + `translation_status`. Degrada sin la migración.
- **Migración `0020_form_answer_translation.sql`** (additive: `source_language`, `answers_translated`, `translation_status`) — **APLICADA a prod 2026-06-18** vía Management API (token de Henry; NO registrada en `schema_migrations` a propósito — es idempotente `IF NOT EXISTS`, re-aplicar = no-op). `database.types.ts` actualizado a mano para coincidir. _Henry: rotar el access token compartido._
- **Code-review (two-stage)**: veredicto NEEDS REVISION → aplicados los 4 SHOULD-FIX (PII a Gemini sin `maskPii`; gate de traducción robusto que nunca deja el PDF en idioma equivocado en silencio; race anti-doble-clic en activar formulario; reset de slug al cambiar de fase) + nits.
- **Gates**: tsc 0 · lint 0 (el `eslint . --max-warnings=0` falla solo si está presente el `public/sw.js` generado por `build` — artefacto untracked, no código; `eslint src` limpio) · vitest **1289** (+12: bridge translator, masking PII, rama de traducción de PDF) · build ✅ (ojo: `next build` con `next dev` corriendo en paralelo corrompe `.next` → matar dev antes de build) · check:i18n OK (1671).
- **PENDIENTE de verificar en vivo** (necesita setup): E2E completo de traducción = formulario `pdf_automation` con versión publicada (subir AcroForm real + propuesta IA + publicar) + caso de cliente en esa fase llenándolo en español → PDF en inglés. Cubierto por unit tests; falta el flujo navegador end-to-end (costo IA real).

### Verificación funcional "todo UI funcional, nada hardcodeado" (pedido de Henry) ✅ — 1 bug grave + 3 botones muertos corregidos + VERIFICADO EN VIVO
Barrido de cada rol/pestaña/botón (Playwright + Supabase MCP, sesión María `…101`, org `a3e5f333`). Hallazgos y fixes:
- **BUG GRAVE — detalle de servicio `/servicios/[slug]` daba 404 para TODOS los servicios.** Causa raíz: el embed PostgREST `service_phases(...)` en `catalog/repository.ts:getServiceDetailBySlugFromDb` era **ambiguo** — hay 2 FKs entre `services` y `service_phases` (`service_phases_service_id_fkey` y `services_entry_phase_fk`) ⇒ PostgREST respondía **HTTP 300 Multiple Choices** ⇒ el repo lanzaba ⇒ la page hacía `notFound()`. Fix: hint de constraint `service_phases!service_phases_service_id_fkey(...)`. Verificado: **14/14 detalles → 200** (asilo-politico, visa-juvenil, taxes, …) + slug inexistente → 404 (control). La lista `/servicios` (que solo embebe `service_plans`) nunca falló, por eso el barrido anterior no lo cazó.
- **Botón muerto #1 — launcher "Tu equipo" del shell de cuenta** (`account-chrome.tsx`) tenía `onClick={() => {}}` (TODO O1). Cableado: el layout `(cliente)` resuelve el caso primario del cliente (server-side) y `AccountChrome` monta el `ChatSheet` apuntando a ese caso. Verificado: abre el hilo real del caso 301.
- **Botones muertos #2/#3 — "Preguntar por mensaje" (`service-detail`) y "Preguntarle a mi equipo" (`corregir`)** tenían `onClick={() => {}}`. Nuevo `messaging/team-chat-bus.ts` (pub/sub de módulo, sin DOM → no viola RNF-036): los screens llaman `openTeamChat()`, las chromes (account/case) se suscriben y abren el overlay O1. Verificado: service-detail abre el chat; corregir por equivalencia (mismo bus → case-chrome, cuyo launcher se confirmó abriendo el chat en ruta de caso).
- **Deferrals deliberados (NO bugs, documentados):** "Me interesa" en service-detail = deshabilitado "Disponible pronto" (flag `NEXT_PUBLIC_FEATURE_INTERES`, lead PS-3 → F3). Mi Historia `/caso/…/historia` "muy pronto" = empty-state data-dependiente (fase sin formulario `ai_letter`, nunca 500). Dashboard admin analytics "Próximamente" = charts diferidos. Estados vacíos (validaciones detalle, avisos) = grácil con CTA.
- Gates: tsc 0 · lint 0. Sin errores de consola en todas las pantallas probadas.

### FIX visual — superficies transparentes (chat, modal PWA, tarjetas) ✅ (reporte de Henry: "se ve sin fondo, transparente") — VERIFICADO EN VIVO
Henry reportó la mensajería y el modal de instalar PWA "sin fondo". **Causa raíz** (token CSS): en `shadcn-theme.css` el bloque `[data-theme]` redefine `--card` con `var(--panel, var(--card-surface, var(--card)))` — **auto-referencial** (`--card` se referencia a sí misma en el fallback) y `--card-surface` nunca estaba definida ⇒ ciclo CSS ⇒ `--card` resolvía a **string vacío** en todo el scope cliente (móvil). Como el bloque shadcn `[data-theme]` (specificity 0,1,0) gana por orden sobre el `[data-theme="light"]` de tokens (0,1,0), envenenaba `--card`, arrastrando `--popover` (línea 19) y **toda** superficie con `var(--card)`: `BottomSheet` (chat, hojas), `PwaInstallPrompt`, tarjetas del dashboard, etc. (`--bg`/`--line` no se afectaban porque shadcn no las redefine). El staff no se afectaba: su bloque `[data-theme="light"].surface-staff` (0,2,0) gana sobre shadcn.
**Fix:** (1) `--card-surface` definida en los themes móviles (`#ffffff` light / `#172238` dark) en `tokens.css`; (2) en `shadcn-theme.css` roto el ciclo: `--card: var(--panel, var(--card-surface))` y `--popover: var(--panel, var(--card-surface))` (sin el `var(--card)` auto-referencial). **Verificado en vivo:** `--card`/`--card-surface`/`--popover` → `#ffffff`; chat con fondo blanco sólido, modal PWA sólido, `/config` y tarjetas del dashboard sólidas. Auditoría de tokens de superficie en cliente: ninguno usado resuelve vacío (los vacíos `--panel`/`--chip`/`--sidebar`/`--line-2` son tokens **staff**, correctamente ausentes en móvil y con fallback).

### Re-sweep STAFF en vivo (login real por rol, viewport 1440×900) ✅
Tras los fixes, barrido de los 4 paneles staff (login real `signInWithPassword`):
- **Superficies staff sólidas confirmadas en vivo**: en `.surface-staff`, `--panel`/`--card`/`--sidebar`/`--popover` → `#ffffff` (el bloque `[data-theme="light"].surface-staff` (0,2,0) gana sobre el ciclo de shadcn (0,1,0) — el fix de token no afectó staff).
- **Admin (Henry)**: 11 rutas `/admin/*` → 200 (incl. `casos/[caseId]` + `/formularios`). Dashboard, detalle de caso (ULP-2026-0001 con cuotas + partes) y catálogo renderizan sólidos con datos reales.
- **Finanzas (Andrium)**: 8 rutas → 200 (incl. `campanas/[id]`, `pagos/caso/[caseId]`). Kanban Cobranza sólido.
- **Legal (Diana)**: 7 rutas → 200 (incl. `validaciones/[caseId]` ×2, `expediente/[caseId]` ×2). Kanban Mis casos sólido.
- **Ventas (Vanessa)**: 8 rutas → 200 (incl. `clientes/[caseId]`). Panel Mi día sólido.
- **Role-gating OK**: navegar a `/admin` como finance redirige a `/finanzas` (no over-privilege).
- **0 errores de consola** en los 4 roles. 34 rutas staff, todas 200, sin 404/500/redirects inesperados. (Nota de testing: el fetch-sweep concurrente rota el refresh-token de @supabase/ssr y tira la sesión — artefacto de prueba, no bug; la navegación normal por página funciona.)

### FIX UX panel staff — nav doble-activa + spacing del main + responsive móvil + apariencia en config (reportes de Henry) ✅ VERIFICADO EN VIVO
1. **Nav doble-activa (bug "dos secciones con focus"):** dos items de `STAFF_NAV` apuntaban al mismo `href="/ventas/citas"` ("Calendario" en Operación + "Citas" en Ventas) ⇒ `isActive` marcaba **ambos** `data-active` (dos pills iluminadas). Único href duplicado del árbol. Fix en `Sidebar`: resolver un **único** activo (match más específico por longitud de href; en empate gana el último = el que "posee" la URL → "Citas"). Sin tocar la IA (ambos items siguen). Verificado: en /ventas/citas solo "Citas" activo.
2. **Spacing del `<main>`:** el `StaffShell` tenía `<main>` con padding 0 (el preview de dev sí padeaba `20px 26px 60px`; el shell real lo perdió). Restaurado en `staff-shell.css` como `padding: 22px clamp(16px,3vw,30px) 56px` → el contenido respira.
3. **Responsive móvil (panel admin en teléfono):** nuevo `staff-shell.css` — ≤860px la sidebar de 248px colapsa en **drawer off-canvas** (toggle hamburguesa en topbar, scrim, cierre al navegar vía `usePathname`), el contenido va a ancho completo y los grids densos apilan (≤560px). `Sidebar` acepta `open`→`data-open`; `StaffShell` maneja el estado + scrim; `Topbar` añade el botón hamburguesa (SVG inline). Verificado a 390px: drawer abre/cierra, sin scroll horizontal.
4. **Apariencia (tema + tamaño de texto) movida a Configuración en móvil:** el `ThemeToggle` del topbar (tamaño A−/A/A+ + Claro/Oscuro) desbordaba a 390px → **scroll horizontal**. Ahora se oculta ≤860px (`.staff-topbar-appearance`) y vive en Configuración: nuevo `StaffAppearanceCard` (envuelve `ThemeToggle`) añadido a admin/legal/finanzas configs (+ claves i18n `staff.config.appearance`/`appearanceSub`); a Vanessa (config propia) se le añadió el control de **tamaño de texto** a su card de Apariencia existente. Desktop intacto (controles siguen en topbar, sidebar sticky). Verificado: a 390px `docScrollW 375 ≤ 390` (sin scroll), apariencia visible en config.
5. **Dark mode verificado en vivo:** staff `--card`/`--card-surface`/`--popover` → `#172238`, `--panel` `#101e34`, `--sidebar` `#0c1830` (ninguno vacío); superficies sólidas en dark (chat/cards/paneles).
6. **BONUS — 2 errores de consola pre-existentes corregidos** en `/admin/configuracion`: `buildConfigStrings` formateaba con `t()` claves ICU con placeholder (`termsPublished`="Publicada el {date}", `termsAcceptedBy`="{n} aceptaciones") → `FORMATTING_ERROR`. Ahora usan `t.raw()` (las sustituye `ConfigView`). Verificado: 0 errores.
- Gates: tsc 0 · lint 0 · i18n OK (1651 paridad).
7. **Admin veía 4 "Configuración" (1 por depto):** cada departamento tiene su config personal (idioma/apariencia) gateada por su módulo → un rol ve solo la suya, pero el admin (bypass) veía las 4 (legal/ventas/finanzas/admin), todas etiquetadas "Configuración". Fix: flag `hiddenForAdmin` en los 3 configs de departamento (`filterNav` ahora recibe el item completo); el admin conserva solo su "Configuración" org-wide (`settings`, con org+idioma+apariencia). No-admins sin cambios. Verificado: admin pasa de 4→1 entrada "Configuración".

### Ola F8-8g — Go-live prep (DOC-82 §8) ✅ (código-ready + runbook)
Artefactos creados + verificados: **`vercel.json`** (framework nextjs + región iad1 — verificar vs región Supabase) · **`.vercelignore`** (excluye docs/e2e/tests) · **`scripts/provision-schedules.mjs`** (aprovisiona los 6 crons QStash idempotente; API `schedules.list/create/delete` verificada) · `route.ts` QStash `export const maxDuration = 300` (jobs IA largos) · gate **`check:assets`** añadido al job quality del CI · **`GO-LIVE.md`** (runbook completo: 35 ítems DOC-82 §8 con estado código✅/Henry⬜, env vars Vercel tier1+tier2, bootstrap BD + seeds 01/02, comando schedules, webhooks a registrar, **flip CSP a enforcing tras ventana Report-Only**, demo de salida). Gates: tsc 0 · lint 0. **Pendiente (acciones Henry/externas, en el runbook)**: deploy prod + DNS `app.usalatinoprime.com` + 25 env vars + crear 4 staff + correr seeds/schedules + registrar webhooks (Stripe/Resend/LiveKit/Abogados) + Twilio/SMTP + monitor + MFA + precios + texto legal + demo final. Plan completo en `~/.claude/plans/analiza-mi-proyecto-y-twinkly-reddy.md`. F7 ✅ (7a chat · 7b notificaciones+push+avisos · 7d comunidad; 7c LiveKit diferida). F0–F6 ✅ (F6 desplegada en `https://x-legal-nine.vercel.app`).

### Ola F8-8f — Performance + a11y (RNF-001/033/034/035/037) · EN CURSO
- **✅ Lex optimizado (RNF-035)**: `lex.gif` **9.1MB→29KB** (gif estático de primer frame; el webp animado lo usan todos los navegadores modernos, el gif es solo fallback) + `lex.webp` **1024KB→980KB** (re-encode q50 vía sharp, 272px, calidad intacta — verificado en /welcome). Precache revision `lex.webp` v1→v2.
- **✅ Asset-size gate**: `scripts/check-asset-size.mjs` + `npm run check:assets` (falla si algún `public/*` > 1MB; ignora `sw.js`/`swe-worker` generados). OK ahora.
- **✅ Bundle budget (RNF-035)**: First-Load-JS shared **104KB < 300KB** (ya verificado en el build de 8b).
- **✅ a11y contraste WCAG AA (decisión Henry: fijar)** — verificado con axe-core en vivo, **light + dark, client + staff**: `--ink-3` (gris muted) #94a2b8→**#647088** (2.4→4.6) light, #64768f→**#7c8da6** dark; `--accent`/`--brand-blue` #2f6bff→**#2a63f2** (4.2→4.7); staff dark `--ink-3` #6c82a6→**#8294b3**. **Resultado axe**: welcome/email **0 contraste**, /admin de 15→**1** nodo (= el wordmark "PRIME" dorado del logo, **exento por WCAG 1.4.3** logotype), dark **0** en todas. **0 críticos/serios reales.** Sin regresión visual (captura — el nudge del azul y el muted más oscuro son imperceptibles/sutiles). _(El gate axe `A11Y_GATE=1` debe excluir el elemento del logo.)_
- **Pendiente (go-live/CI)**: Lighthouse CI gate (los budgets ya se cumplen: First-Load-JS 104KB<300KB + assets<1MB; falta solo el setup de `lhci` en CI).

### Ola F8-8e — Seguridad (CSP + headers + RLS 30/30 + OWASP) ✅ (CSP/RLS/migración hechos + verificados)
SoT: DOC-27 §6 (headers/CSP) + DOC-31 §8.2 (30 tests RLS) + advisors Supabase.
- **✅ CSP + 5 headers** (verificado en vivo, gates verdes): **5 headers estáticos enforcing** en `next.config.ts headers()` (HSTS `max-age=63072000; includeSubDomains; preload` · X-Content-Type-Options nosniff · Referrer-Policy strict-origin-when-cross-origin · Permissions-Policy `camera=(self),microphone=(self),geolocation=(),payment=()` · X-Frame-Options DENY). **CSP nonce-based Report-Only** en `src/middleware.ts` (`Content-Security-Policy-Report-Only`, nonce per-request `crypto.randomUUID`, `script-src 'self' 'nonce-…' 'strict-dynamic'`, connect-src Supabase+LiveKit, form-action Stripe, frame-ancestors/object-src 'none'). Next auto-inyecta el nonce a sus scripts (lee la CSP del request header que setea el middleware). **VERIFICADO**: curl muestra los 6 headers; /admin autenticado (sesión/cookies NO se rompieron pese a tocar el cookie-forwarding del middleware); 36/38 scripts con nonce; **0 errores de consola** (resuelto un hydration-mismatch quitando el nonce manual del THEME_INIT_SCRIPT — Next lo maneja). tsc 0 · lint 0. _(TODO go-live: flip a enforcing `Content-Security-Policy` tras la ventana Report-Only + nonce+suppressHydrationWarning al THEME_INIT_SCRIPT.)_
- **✅ RLS tests 14→30** (subagente + verificado por mí): **9 archivos nuevos** `14_*`…`22_*.sql` (child-table isolation, form-approval gate, webhook_events service-role-only, calls, notifications, community read-only, scheduling scope, storage bucket policies, realtime+multi-tenant) → **30/30 escenarios DOC-31 §8.2 cubiertos** (22 archivos totales). Schema-verificado vía MCP (nombres tabla/columna/CHECK reales). **+ 2 bugs PRE-EXISTENTES arreglados** que romperían la suite: `11_*` insertaba `employee_module_permissions(user_id, module)` → `(staff_id, module_key)`; `09_*` tenía UUIDs con `i` no-hex → `e`. _(`npm run test:rls` = `supabase test db` necesita Docker/stack local — NO disponible aquí; los tests quedan correctos para CI.)_
- **✅ Hardening migración APLICADA** (autorizada por Henry, `f8_8e_security_hardening`): revocado EXECUTE de `merge_form_answers(uuid,jsonb)` + `next_case_number(uuid)` a anon/authenticated + deny-all explícito en `_case_number_counters`. **Verificado vía `get_advisors`**: esos 3 findings (merge anon+auth, next_case_number auth, counter no-policy) **eliminados** → advisors **13→9**, **0 críticos**. **Remanentes (WARN, no tocados a propósito)**: helpers RLS `is_admin`/`is_staff`/`is_client`/`is_case_member`/`is_conversation_participant`/`has_module` (revocar rompería las policies; devuelven self-status, bajo riesgo); `pg_trgm`/`btree_gist` en `public` (mover = riesgoso por índices dependientes, diferido). **Henry (dashboard)**: leaked-password protection + MFA TOTP admin.
- **OWASP A01-A10**: posture = advisors 0 críticos + protecciones existentes F0-F7 (RLS, PII AES-256-GCM, firmas webhook, rate-limit, sanitización HTML, audit append-only) + nuevos headers/CSP. (Pase dedicado `/cso`/security-auditor opcional.)

### FIX tipografía (reporte de Henry: "todo rinde en system-ui") ✅ — VERIFICADO EN VIVO
**Causa raíz:** la variable de next/font `--font-plus-jakarta` estaba en `<body>`, pero los tokens `--font-title`/`--font-body` se definen en `:root` (`<html>`) y referencian `var(--font-plus-jakarta)`. Como el var no existía en `:root` (estaba en un descendiente), `--font-title` resolvía a **inválido/vacío** → heredaba vacío → **todo caía a system-ui** (y `--font-body` estaba literalmente puesto a `system-ui`, desviándose del prototipo que usa Plus Jakarta para título Y cuerpo). **Fix (3):** (1) `layout.tsx` mueve `plusJakartaSans.variable` de `<body>` a `<html>`; (2) `fonts.ts` añade pesos 400/500 (cuerpo) a los 600/700/800; (3) `tokens.css` `--font-body` usa `var(--font-plus-jakarta)`. **Verificado:** body/h1/p computan **Plus Jakarta Sans** (antes system-ui); captura del dashboard confirma. tsc/lint/i18n verdes.

### Ola F8-8d — platform-bridge / Capacitor-ready (RNF-036) ✅ (subagente + verificado por mí)
SoT: DOC-24 §3+§5. **Módulo `src/frontend/platform-bridge/`** (antes vacío): `index.ts` (interfaz `PlatformBridge`: camera/files/dictation/push/share/haptics + `getBridge()` singleton SSR-safe, selección web/capacitor por `window.Capacitor`, import dinámico del stub), `web.ts` (impl web de las 6 capacidades; lógica de push **movida verbatim** de push-helpers), `capacitor.ts` (stub que compila). **Refactor de 11 violaciones** en 9 archivos detrás del bridge: push (`use-push-notifications`→`bridge.push`; `push-helpers` ahora re-export), dictation (`form-wizard/use-dictation`→`bridge.dictation`), enlaces tel:/wa.me (vanessa leads/mi-dia→`bridge.share.openExternal`), descargas PDF ×4 (case-forms/ensamblador/chat-thread/documentos-tab→`openExternal`), clipboard ×2 (new-case-modal/pagos-caso→`bridge.share.copyText`). **Regla ESLint RNF-036** en `eslint.config.mjs` (`no-restricted-globals`/`no-restricted-properties` en `src/frontend/features/**`: prohíbe SpeechRecognition/Notification/PushManager + `navigator.{mediaDevices,share,vibrate,clipboard,serviceWorker}`/`window.open`) → **0 violaciones residuales**. NO tocados (válidos): `window.location.reload`/href relativo, redirect Stripe, localStorage de prefs UI. **TODO(8d)**: camera/files en `upload-screen` (swap invasivo del flujo signed-URL+XHR verificado; bridge disponible para migración). **VERIFICADO por mí**: tsc 0 · eslint 0 (RNF-036 pasa) · check:i18n 1648 OK; bridge files presentes; push-helpers re-export OK; /admin/casos (clipboard refactor) y /legal/ensamblador (share refactor) renderizan sin errores de consola.
Última sesión: 2026-06-17

### Ola F8-8a — Shell móvil: bottom nav fija + safe-areas + responsive ✅ (construido + VERIFICADO EN VIVO + gates verdes)
SoT: DOC-24 §5.5 (safe-areas, no-`100vh`). **Pedido #1 de Henry:** "barra inferior fija + que no la tape la barra del dispositivo móvil".
- **Causa raíz:** `bottom-nav.tsx` se renderizaba `position: absolute` (ambos chromes pasaban `absolute`) → anclado al contenedor, **no al viewport** → en páginas largas la barra se iba con el scroll. `paddingBottom: 22` fijo **sin** `env(safe-area-inset-bottom)` → tapada por la gesture-bar. Root layout **sin** `viewport-fit=cover` → `env()` ni aplicaba.
- **Fix:** (1) `src/app/layout.tsx` → `export const viewport` con `viewportFit:'cover'`; (2) `globals.css` → vars `--safe-top`/`--safe-bottom`=`env(safe-area-inset-*,0px)` + `--screen-pb`=`calc(120px + var(--safe-bottom))`; (3) `bottom-nav.tsx` → `paddingBottom: calc(22px + var(--safe-bottom))` (la rama fija ya centraba en 430px vía `left:0;right:0;margin:0 auto`); (4) `account-chrome.tsx`+`case-chrome.tsx` → **quitado `absolute`** (fixed real); (5) `fab.tsx` (MessagingLauncher+LexFab) → `bottom: calc(104px + var(--safe-bottom))`; (6) **paddings-bottom de ~20 screens** cliente (120/116/124/132/140/96px hardcodeados) → centralizados a `var(--screen-pb)` (consistencia + safe-area). FormWizard cubre formulario/historia.
- **VERIFICADO EN VIVO** (Playwright 390×844): nav variante **cuenta** (servicios) y **caso** (camino) → `position: fixed`, `bottom:0`, pinned al viewport; en /servicios (scrollHeight 1814) tras scrollear 970px **sigue pinned** (`bottom===innerHeight`); simulando notch (`--safe-bottom:34px`) → nav paddingBottom **56px**, launcher bottom **138px**, screen-pb **154px** (sin solape); light+dark OK; **0 errores de consola**. (Nota: el botón "Continuar" del wizard es inline, no footer fijo.)
- Gates: **tsc 0 · eslint 0** (solo warnings pre-existentes del plugin boundaries). _(vitest/i18n/build se corren al cierre de la ola con el dev server detenido.)_

### Ola F8-8b — PWA completa (Serwist + manifest + offline + install/update) + push ✅ (construido + VERIFICADO + gates verdes)
SoT: DOC-24 §2. **Pedido #2 de Henry:** "PWA + push bien implementadas y testeadas".
- **Serwist** (`serwist`+`@serwist/next` 9.5.11): `src/app/sw.ts` (`/// <reference lib="webworker" />` + precache `self.__SW_MANIFEST` + **6 estrategias §2.3**: Storage/signed-URLs `token=` = NetworkOnly·webhooks/auth = NetworkOnly·navegaciones = NetworkFirst 3s→/offline·GET `/api/v1` = NetworkFirst 4s maxAge 300 maxEntries 64·imágenes = SWR maxEntries 96; plugin `respectNoStore` para `Cache-Control: no-store`; **conserva los handlers push/notificationclick de F7**; `skipWaiting:false`+`clientsClaim:true`). `next.config.ts` → `withSerwistInit({swSrc, swDest:'public/sw.js', disable: dev, register:false, additionalPrecacheEntries:[/offline.html, /assets/lex.webp]})`.
- **⚠ FIX build:** `build` usaba `next build --turbopack` → el plugin **webpack** de Serwist **no corre** bajo turbopack (no generaba el SW). Cambiado a `next build` (webpack). Destapó un **bug latente**: `api/webhooks/qstash/[job]/route.ts` hacía `export { JOB_REGISTRY }` (export no permitido en route handlers; turbopack lo toleraba, webpack falla la validación de tipos) → **eliminado** (export huérfano, ningún test lo importaba).
- **manifest.webmanifest** (§2.2 exacto: name/short_name/standalone/portrait/theme #002855/bg #F4F7FB/categories/4 iconos) + `metadata.manifest` + `<meta theme-color>` light `#F4F7FB`/dark `#0c1322` + apple-touch-icon + viewport-fit=cover (de 8a). **5 iconos generados** vía `scripts/generate-icons.mjs` (sharp, SVG sin fuentes: cuadrado gradiente navy→accent + "U" blanca + chispa dorada): icon-192/512 (any), maskable-192/512 (safe-zone 78%), badge-72.
- **/offline** estático `public/offline.html` (la ruta React era `ƒ` dinámica por `getLocale()` del root layout → **no precacheable**; se reemplazó por HTML estático self-contained, branded + bilingüe vía cookie `ulp-locale` + Lex, precacheado por `additionalPrecacheEntries`).
- **Update banner** (`pwa-update-banner.tsx`, toast superior, escucha `waiting`→`SKIP_WAITING`→`controllerchange`→reload) en (cliente)/layout. **Install prompt** (`pwa-install-prompt.tsx`, `beforeinstallprompt` + guía iOS + guard standalone/Capacitor, dismiss persistente) en AccountChrome. i18n `pwa.update`/`pwa.install` es/en. **Logout purge** (config `handleSignOut`): desuscribe push + `caches.delete(['api-v1','pages'])` conservando precache.
- **VERIFICADO**: dev → manifest 200/válido (4 iconos, standalone, theme #002855), iconos 200, theme-color light/dark, apple-touch-icon, SW registrado, /offline render OK. **Build webpack** → `✓ (serwist) Bundling '/sw.js'` + 29/29 páginas + First Load JS **104 kB** (<300 KB). **Prod (`next start`)** → SW Serwist **activado+controlando**, precache 155 entradas + `/offline.html` precacheado; **fallback offline FUNCIONAL** (offline + navegar /home → sirve `/offline.html` desde precache, título "Sin conexión", contenido bilingüe — captura). Push: pipeline 1277 tests ✅ + handler `showNotification` presente en sw.js prod + `push_subscriptions` (7 cols, RLS) ✅. _(Entrega push real device→FCM→OS = verificación de release: necesita HTTPS prod + VAPID en Vercel + dispositivo, DOC-24 §2.2.)_
- **Ops SW dev/prod**: `public/sw.js` commiteado = push-only (Serwist disabled en dev). Prod build lo **sobrescribe** con el de Serwist → restaurar antes de volver a dev (`.gitignore`: `sw.js.map`/`swe-worker-*`). Tras esta verificación: SW dev restaurado, `.next` limpio, dev:e2e reiniciado en :3000 (Henry: hard-refresh por cambio de hash CSS).
- Gates: **tsc 0 · eslint 0 · vitest 1277 · i18n 1528 · build 0 (webpack)**.

### Ola F8-8c — QA visual + auditoría bilingüe ES/EN · EN CURSO
SoT: DOC-24 i18n. **Pedido #3 de Henry:** todo el sistema (cliente + admin) visible en es/en, switch en config por rol; **decisiones:** persistir `users.locale` + selector en config de cada rol staff.
**Parte A — fixes i18n (hechos + verificados):**
- **Persistencia `users.locale`** (antes solo cookie): nuevo `updateUserLocale` (repo) + `setUserLocale` (service, valida es/en) + **`setUserLocaleAction`** (identity actions: `requireActor`→BD→cookie `ulp-locale`). Wireado en **cliente** (`/config` `onLang`→action) y **staff ventas** (`/ventas/configuracion`→`setUserLocaleAction`). **VERIFICADO EN VIVO** (Playwright + Supabase MCP): María cambia ES→EN en /config → UI re-renderiza en inglés (`htmlLang=en`, "Settings/Notifications") + **`users.locale='en'` en BD**; toggle EN→ES revierte (`locale='es'`). Los emails/push ahora siguen el idioma elegido.
- **Push strings → catálogo**: los 4 ternarios inline de `config-screen.tsx` (Notificaciones push/Avísame/No disponible/Permiso bloqueado) → claves `cliente.config.notifPush*` (es/en) + `ConfigLabels`.
- **`check-i18n-keys.mjs` endurecido**: ahora falla si una clave tiene **valor vacío** en es o en (con allowlist documentada para 3 `*.fallbackName` intencionalmente vacíos). OK 1532 claves.
- **Selector de idioma por rol staff** (decisión Henry): componente compartido `StaffLanguageCard` (token-styled, usa `setUserLocaleAction`) montado en **admin/configuracion** (card bajo ConfigView) + **nuevas páginas** `/finanzas/configuracion` y `/legal/configuracion`. Nav: `financeConfig` (gate `accounting`), `legalConfig` (gate `validations`) + se **limpió el clutter** pre-existente (salesConfig `dashboard`→`leads`, settings `dashboard`→`employees`) → cada rol ve **solo su** Configuración. Datos BD verificados (finance/paralegal/sales tienen dashboard; gateo por módulo único de rol). i18n `staff.config.*` + `staff.nav.items.financeConfig/legalConfig` (es/en).
- **VERIFICADO EN VIVO como Andrium (finance, login real)**: /finanzas/configuracion renderiza el card "Idioma" (Español/English); sidebar muestra **1 sola** "Configuración" (FINANZAS); toggle ES→EN → **todo el panel staff en inglés** ("Settings/Cases/Case files") + **`users.locale='en'` en BD**; revierte a `es`.
- Gates Parte A: **tsc 0 · eslint 0 · check:i18n 1540 OK (paridad, sin vacíos)**. _(Nota: `ventas/actions.ts setLocaleAction` quedó sin uso — reemplazado por `setUserLocaleAction`; candidato a borrar.)_
**Parte B — auditoría bilingüe pantalla-por-pantalla (EN CURSO):**
- **Bloque CLIENTE ✅ LIMPIO**: sweep automatizado de 15 pantallas en inglés (cookie `ulp-locale=en`, sesión María) + log del server → **0 raw keys · 0 fugas de español · 0 errores i18n** (consola + server). Home verificado visualmente en EN (idéntico, traducido). 
- **Bloque STAFF (sesión admin Henry, EN)** — sweep de 25 rutas:
  - admin (claves next-intl) + ventas/finanzas (inline `tt()`) → **bilingües OK**; flags menores (`expediente` en `/admin/auditoria`, `con`/`las`/`nuevo` sueltos) = **datos semilla en español** (esperado), no UI rota.
  - **🔴→✅ Panel `legal`/Diana internacionalizado** (estaba hardcodeado en español, sin i18n). **FIX hecho** (subagente, 8 archivos): cableadas `useTranslations`/`useLocale` en validaciones-list + validaciones-detail + ensamblador; consts de módulo (`STATUS_PILL`/`FILTER_TABS`) refactorizados de `label`→`labelKey` resueltos en render; relative-time/fechas locale-aware (claves `relMinAgo`/`relHrAgo` + `toLocaleDateString` es-PE/en-US). Namespace **NUEVO `staff_ensamblador`** (62 claves es/en) + `staff_validaciones` extendido (+42) + `staff.legal.kanban` (+7, el kanban ya estaba i18n). **VERIFICADO EN VIVO** (sesión Henry, ES+EN): validaciones ("Validations/All/In progress/Validated/Green light") + ensamblador ("Expediente assembler/Create expediente") renderizan en inglés; ES intacto; **0 raw keys · 0 errores en ambos idiomas**. Gates: **tsc 0 · eslint 0 · check:i18n 1648 OK**.
  - **Terminología normalizada (decisión Henry)**: "Expediente"→**"Case file"** en los **valores EN** (30 valores, solo en.json; claves `errExpediente*`/`goToExpediente` y el ES intactos) → consistente con el nav. Verificado en vivo: "Case file assembler", "All case files sent…", 0 "expediente" en inglés. check:i18n 1648 OK.
- **✅ Ola 8c COMPLETA + verificada**: cliente (15 pantallas) + staff (admin/ventas/finanzas/legal) bilingües ES/EN; persistencia `users.locale`; selector por rol; push strings al catálogo; check:i18n endurecido; panel legal internacionalizado. _(Nit: `ventas/actions.ts setLocaleAction` sin uso — candidato a borrar.)_

### Ola F8 — QA visual previo (movido a 8c) · 3 bugs UI corregidos + VERIFICADOS EN VIVO
- **i18n raw keys (FORMATTING_ERROR)** — clase completa: `caso/[caseId]/camino/page.tsx` (encourage/phaseChip/nextDocsTitle/documentsValue/formsValue) + `caso/[caseId]/documentos/page.tsx` (subtitle) pasaban claves con placeholders ICU por `t()` cuando la pantalla hace su propio `.replace()` → render en crudo. Fix: **`t.raw(...)`**. Verificado (light+dark); 0 FORMATTING_ERROR en el log.
- **hydration nested-button** en `/config` toggle Modo oscuro: `<button>`→`<div>` (el Switch es el único control). 0 errores.
- Pendiente (→ 8c): auditoría visual sistemática cliente+staff vs prototipos + bilingüe ES/EN.

### Ola F7-7d — Comunidad (feed + reacciones + banner EN VIVO + moderación staff) ✅ (construido + VERIFICADO EN VIVO + gates verdes; code-review en curso)
SoT: DOC-30 §12 (community_posts/reactions/comments) + DOC-10 RF-CLI-055..058 (acceso gratis solo-clientes; feed texto/video; banner EN VIVO; reacciones heart/fire/clap toggle). **Cero migraciones** (0012_community.sql ya aplicada: tablas + RLS — clientes leen publicados de su org, staff con módulo `community` escribe/modera). Decisión Henry: 7d elegida tras cerrar 7a/7b; 7c (LiveKit) diferida.
- **Módulo `src/backend/modules/community/`**: domain (`isLivePostActive` ventana 2h, `aggregateReactions` counts+mine, `isReactionKind`); repository service-role (`listPublishedPosts` keyset, `listAllPosts` moderación, `setPostPublished`, `findActiveLivePost`, `listReactionsForPosts`, `find/insert/deleteReaction` [insert tolera 23505], `insertPost`); service (`canViewCommunity` [cliente OR staff admin/módulo] → `getFeed` [posts publicados + banner live excluido del listado + reacciones agregadas], `toggleReaction` [valida kind+publicado+**org match**→insert/delete→recomputa], `createPost` [can edit; author=actor o testimonial], `listPostsForModeration`/`setPostPublished` [can view/edit + audit]); `CommunityError`; `actions.ts`.
- **UI cliente** `/comunidad` (antes 404 — el nav ya enlazaba): `CommunityFeed` — header emocional "No estás solo/a", **banner EN VIVO** (navy+halo dorado, badge rojo pulsante con hora localizada, CTA "Entrar a la sesión"), feed texto/video (iframe), reacciones heart/fire/clap con **toggle optimista reconciliado**. **UI staff** `/admin/comunidad`: `CommunityAdmin` (form crear text/video/live + lista moderación con toggle publicar). Nav staff "Comunidad" (módulo `community`) + i18n es/en.
- **VERIFICADO EN VIVO** (Playwright + Supabase MCP): Henry (admin) **crea 2 posts** (texto + live) vía /admin/comunidad + lista de moderación; María (cliente) **ve el feed** con el **banner EN VIVO** ("EN VIVO · mié 7:00 p.m." + CTA) + el post de Henry; **reacción heart toggle** on→1 (activa) / off→0, **persiste en BD**.
- **🛡️ Two-stage review** (code-reviewer sobre los 8 archivos) → **NEEDS-REVISION** (1 BLOCKER + 2 STRONG + 2 nits) → **corregido**: **BLOCKER** `video_url`/`live_join_url` sin validar protocolo (texto libre staff → `<iframe src>`/`<a href>`; un `javascript:`/`data:` ejecutaría XSS) → **`createPost` rechaza no-http(s)** (`INVALID_URL`); STRONG-1 `isLivePostActive` sin cota inferior (un live a semanas mostraba el banner hoy) → **ventana `[start - 24h, start + 2h]`** (`LIVE_LEAD_MS`); STRONG-2 `deleteReaction` tragaba errores de BD → **lanza**; nit cap de `limit` en `getFeed` (`Math.min(…,50)`). + verification-before-completion (verificado en vivo). **Verdict tras fixes: APPROVED.** Reviewer destacó: org-isolation completo, `canViewCommunity` correcto, body como texto plano (sin XSS), 23505 tratado como éxito, boundaries limpias.
- **VERIFICADO el fix del BLOCKER en vivo**: crear video post con `javascript:alert(document.cookie)` → "Error: INVALID_URL", 0 posts maliciosos en BD.
- Tests (+10 domain: ventana live [incl. cota inferior], isReactionKind, aggregateReactions). Sin nuevas claves i18n salvo `staff.nav.items.community` (es/en). Gates (post-review): **tsc 0 · eslint 0 · vitest 1277 · i18n 1521 · build 0**.
- **Nota operativa ("nav sin css")**: era un artefacto de **caché por port-drift** — reiniciar el dev server muchas veces con `rm -rf .next` cambia el hash del CSS; las pestañas abiertas referenciaban un chunk viejo (404 → sin estilos). Resuelto con un dev server **estable en 3000** (sin más reinicios) + hard refresh. Re-verificado: nav staff + `/admin/comunidad` + `/comunidad` renderizan con CSS correctamente.
- **Diferido (no en RF-CLI-055..058 V2.0)**: comentarios de cliente (la tabla existe; los RF del cliente son read+react, sin comentar) → moderación de comentarios queda como hook para V2.1.

### Ola F7-7b — Notificaciones completas + Web Push + Centro de avisos · EN CURSO (sub-pasos 7b-1/7b-2/7b-3)
SoT: DOC-47 Parte B (pipeline §4.1, anti-ráfaga §4.2, matriz canónica §4.3, servicios §5) + DOC-24 (PWA + Web Push) + DOC-46 §5.3 (contrato anti-ráfaga) + DOC-30 §11 (`notification_preferences`/`push_subscriptions`). **Cero migraciones** (tablas verificadas por MCP).

**7b-1 — Backend de despacho (preferencias + anti-ráfaga + filas F7) ✅ (construido + tests + gates verdes)**
- **Preferencias** (`notification_preferences`, antes nunca leídas): `getPreferences` (defaults todo-true) + **filtro por categoría** (`messages`/`appointment_reminders`/`payment_reminders`/`case_updates` — categoría apagada ⇒ 0 canales, RF-TRX-010 CA2) + **filtro por canal** (`channels {inapp,push,email}`); tipos **◆ no suprimibles** ignoran categoría Y canal (RF-CLI-061). `updatePreferences` (merge sin perder categorías). Cada regla de la matriz ahora lleva `category`.
- **Anti-ráfaga** `message.sent`→`message.received` (§4.2/§5.2, reemplaza el stub no-op de 7a): `notifyMessageBurst` — 1ª msg crea fila in-app + encola push con **delay 5s** (grace; el handler re-verificará `read_at IS NULL` en 7b-2); msgs siguientes dentro de ventana 5min **actualizan el digest** ("N mensajes nuevos…") sin fila nueva ni push; idempotencia `message.sent:{messageId}:{recipient}`; scope por `action_url` por-conversación. Repo: `findUnreadMessageDigest`/`bumpMessageDigest`.
- **Filas F7 de pagos** (ya emitidas + cableadas en register-consumers, antes no-op por falta de fila): `installment.overdue` (CLI ①②③ + AND ①), `installment.paid` (CLI ①③ + AND ①), `payment.proof_submitted` (AND ①② + CLI ①), `payment.refunded` (AND ①) — categorías + emailKeys + deep links según matriz §4.3.
- **Centro/preferencias (servicio)**: `markAllRead`, `getUnreadCount`, `getPreferences`/`updatePreferences` (repo: `getUnreadCountForUser`/`markAllNotificationsRead`/`upsertPreferences`). Consumer `message.sent` cableado → `notifyFromEvent`.
- Tests (+6): gating categoría off (sin fila), canal push off (in-app sí, push no), ◆ ignora prefs, anti-ráfaga (1ª crea+push grace / 2ª bump sin push / messages off suprime). Gates: **tsc 0 · eslint 0 · vitest 1262**. _(build se corre al cierre de 7b para no parar el dev repetidamente.)_

**7b-2 — Web Push ✅ (construido + tests + gates verdes)**
- **Entrega push** (`deliver-notification` rama push, antes stub): re-verifica `read_at IS NULL` (suprime push si ya se leyó — implementa el grace anti-ráfaga DOC-46 §5.3.2); `listPushSubscriptions` → `sendPush` por endpoint con payload localizado (`users.locale`) {title,body,url,tag,icon}; **depura endpoints stale (404/410)** vía `deletePushSubscriptionByEndpoint`; un endpoint que falla no aborta los demás; sin suscripciones ⇒ omite en silencio (RF-TRX-010 CA4).
- **Service worker** `public/sw.js`: `push`→`showNotification` (tag colapsa mismo tipo); `notificationclick`→focus tab existente en `action_url` o `openWindow`; `skipWaiting`+`clients.claim`.
- **Suscripción cliente**: repo `upsertPushSubscription` (onConflict endpoint UNIQUE, service-role para reasignación de dispositivo) / `removePushSubscriptionForUser`; service `registerPushSubscription`/`removePushSubscription`; `actions.ts` del módulo (register/remove push + centro/prefs para 7b-3); frontend `push-helpers.ts` (urlBase64→Uint8Array, `subscribeToPush`/`unsubscribeFromPush`, permission) + hook `use-push-notifications.ts` (boundary-clean, actions inyectadas); `PushSwRegister` montado en el layout `(cliente)` (registra el SW silencioso).
- **Llaves VAPID** generadas (par auto-firmado de la app) y escritas en `.env.local` (gitignored). _Pendiente Henry: añadir el MISMO par a Vercel (como Resend) para push en prod._
- Tests (+5): entrega localizada, supresión por `read_at`, sin-suscripciones, limpieza de stale, resiliencia a un endpoint que lanza. Gates: **tsc 0 · eslint 0 · vitest 1267**.
- _Pendiente para 7b-3 (UI): toggle de suscripción (pide permiso) + desuscripción en logout + verificación push EN VIVO (requiere la UI para conceder permiso)._

**7b-3 — Centro de avisos UI ✅ (construido + VERIFICADO EN VIVO + gates verdes)**
- **Sustrato**: `notifications/types.ts` (VM + `mapNotificationRow` + `NotificationActions`); `use-notifications-realtime.ts` (canal privado `user:{id}` + `postgres_changes` INSERT en `notifications`, `setAuth`+`{private:true}`, degradación a polling 60s); `build-notification-actions.ts` (adaptador raw actions→VM).
- **`NotificationCenter`** (compartido cliente+staff): lista con icono/título/cuerpo/tiempo/punto-no-leído, markRead optimista + navegación al deep link, "Marcar todo como leído", "Ver más" (loadMore), empty state, mapeo de iconos semánticos→vocabulario brand. **`NotificationBell`** (staff topbar): badge de no-leídos + popover (center siempre montado → badge en vivo, cierre por click-fuera).
- **Cliente `/avisos`** (antes 404 — el nav ya enlazaba): `page.tsx` (server: getActor+getNotifications) + `avisos-client.tsx`. **Campana staff**: `topbar.tsx` (`bellSlot`) + `staff-shell.tsx` (`notifications` prop) + layout `(panel)` (carga 1ª página + unread).
- **Preferencias UI**: `/config` toggles cableados a `updatePreferencesAction` (4 categorías) + **toggle push de dispositivo** (usePushNotifications: pide permiso + suscribe) + **logout-desuscribe** (unsubscribe antes de signOut). SW registrado vía `PushSwRegister` en el layout cliente.
- **VERIFICADO EN VIVO** (Playwright + Supabase MCP, dev:e2e): envío Diana→María crea notificación `message.received`; **2º mensaje BUMPEA el digest** ("2 mensajes nuevos de tu equipo", **1 sola fila**, sin push extra); **`/avisos` de María renderiza** la notificación; **markAllRead** → no-leídos 1→0; **preferencia `payment_reminders=false` persiste** (upsert); **campana staff renderiza** (Diana). **Deep link corregido en vivo** (`/caso/{caseId}` base daba 404 → ahora `/caso/{caseId}/camino` cliente · `/admin/casos/{caseId}` staff).
- Tests: las 45 de notifications/job cubren la lógica (gating, anti-ráfaga, push); UI sin tests unitarios (verificada en vivo). Sin nuevas claves i18n (inline `tt()`).
- **🛡️ Two-stage review** (code-reviewer sobre las ~11 archivos de la 7b) → **NEEDS REVISION** (3 issues + 2 STRONG) → **corregido**: (1) `insertNotificationIdempotent` SELECT-then-INSERT podía duplicar bajo retry de QStash → ahora **tolera 23505** (re-lee + `created:false`, no encola push doble; el índice único es parcial `(user_id,dedupe_key)` ⇒ no se puede upsert por PostgREST); (2) `sw.js notificationclick` redirigía cualquier tab → **chequeo de origen** antes de navegar; (3) toggle `inapp` "fantasma" → aclarado (NO se expone toggle inapp; in-app es el canal base siempre insertado, §4.1 paso 6 — comentario añadido); (4) `bumpMessageDigest` sin `user_id` → **filtro `user_id`** añadido; (6) `parseDigestCount` → **cap 999**; (8) log de arranque incluye F7. El STRONG (5) carrera realtime cleanup ya estaba mitigado por el guard `if (cancelled) return` antes de crear el canal. + verification-before-completion (evidencia en vivo ejecutada). **Verdict tras fixes: APPROVED.**
- Gates de toda la 7b (código final post-review): **tsc 0 · eslint 0 · vitest 1267 · i18n 1520 · build 0**.
- **Follow-ups menores (no bloquean)**: badge del bell usa unread de la página cargada (cap "9+" — exacto en la práctica, los no-leídos son los más nuevos); hydration warning PRE-EXISTENTE en el toggle Modo oscuro de `/config` (`<button>` anidado, ajeno a F7); badge en el item "Avisos" del bottom nav cliente (polish); push en prod requiere VAPID en Vercel; nit-7 `buildActionUrl` enrichment global de `newAppointmentId` (riesgo muy bajo, documentado).


### Ola F7-7a — Mensajería: chat cliente↔staff en tiempo real ✅ (construido + two-stage review + VERIFICADO EN VIVO, pendiente OK de Henry)
SoT: DOC-80 §F7 + DOC-46 (módulo messaging: scopes, no-leídos, mensajes de sistema, eventos, errores) + DOC-25 (realtime: canales privados, autorización, degradación a polling) + DOC-30 §10-11 + DOC-48 + DOC-31 (RLS 21-22). **Cero migraciones** (0010 messaging + 0015 realtime ya aplicadas: tablas + RLS + índice único `conversations_one_per_case_idx` + `messages` en publicación `supabase_realtime` + policy `rt conv select`). Decisión Henry: núcleo de comunicación primero (7a chat); LiveKit (7c) y comunidad (7d) diferidas.
- **Paso A — Módulo `src/backend/modules/messaging/`**: domain (`isUnread` RF-TRX-017, `SYSTEM_MESSAGE_TEMPLATES` 5 keys bilingües sin IA, `computeCaseParticipantIds`, `validateAttachmentRefs`); repository service-role (`ensureCaseConversation` idempotente vía índice único insert→on-conflict-select, `markReadMonotonic` `greatest(...)`, `countUnreadAggregate`, keyset `listMessages`, `loadCaseParticipantSources` **org-scoped**); service (`requireParticipant`[org check + participante + override `isStaffAdmin`]/`requireCaseAccess`→Zod→domain→repo→`writeAudit`→emit `message.sent`): `sendMessage`/`getCaseThread`(+`viewerCanPost`)/`postSystemMessage`(sin message.sent/sin notify)/`markRead`/`translateMessage`(cache-first, solo `body_translated`)/`add`/`removeParticipant`(guards CLIENT/LAST_STAFF) + ciclo adjuntos signed-URL (bucket `chat-attachments`, path con prefijo `${conversationId}/`, allowlist + magic bytes). `MessagingError` tipado. actions.ts (`"use server"`).
- **Paso B — Eventos** (`register-consumers.ts`): `downpayment.confirmed`→`ensureCaseConversation`+`postSystemMessage` (try/catch + logger.error); `message.sent`→consumer no-op stub (7b lo reemplaza).
- **Paso C — Realtime cliente**: `supabase-browser.ts` singleton; `use-conversation-realtime.ts` (`setAuth(token)` + `channel('conv:'+id,{private:true})` + `postgres_changes` INSERT en `messages` filtrado por `conversation_id`; **degradación**: 30s sin SUBSCRIBED → polling `loadSince` cada 10s; reconexión re-arma timer [STRONG-1]). Optimistic send + de-dupe por id.
- **Paso D — UI**: cliente overlay `ChatSheet`+`MessagingLauncher` "Tu equipo" en `case-chrome.tsx`/`account-chrome.tsx`; staff tab `mensajes` config-driven en `shared-case` (admin/casos · legal/expediente · ventas/clientes). `ChatThread` compartido: burbujas, sistema centrado, traducir por mensaje, chips de adjunto con descarga, scroll infinito, composer. **Composer gateado por `viewerCanPost`** (no-participante con acceso al caso → banner read-only en vez de composer que falla; banner "Reconectando" también gateado).
- **🐛 Bug cazado en vivo + corregido**: `z.string().uuid()` estricto rechazaba IDs seed (`...-0a01`) → **`zUuid` lenient** (regex 8-4-4-4-12 hex) en 3 schemas. El primer envío en vivo falló con ZodError → diagnosticado vía log → corregido.
- **🛡️ Two-stage review**: code-reviewer previo (BLOCKER cross-org en `loadCaseParticipantSources` admins no org-scoped → fetch case + scope por org; STRONG-1 reconexión realtime; STRONG-2 rechazo de consumer swallowed) **+ review del delta** → **APPROVED** (STRONG helper `isStaffAdmin` compartido entre guard y `viewerCanPost` → extraído; STRONG `translateMessage` orden → analizado **no aplica**: el guard `requireParticipant` SÍ corre antes de IA/write). + verification-before-completion (evidencia ejecutada).
- **VERIFICADO EN VIVO** (Playwright 2 sesiones + Supabase MCP, dev:e2e stub IA, 0 errores de consola messaging): Diana (paralegal participante) **envía → persiste** (`sender`+`kind` correctos) + realtime **SUBSCRIBED** (sin banner degradado); Andrium (finance no-participante) **rechazado `NOT_PARTICIPANT`** + ve **banner read-only**; **realtime entrega a 2ª pestaña SIN recargar** (postgres_changes, sin optimistic en la 2ª); María (cliente) **ve los mensajes de Diana** (lectura cross-user RLS) + responde → persiste; **markRead monotónico** (María 0 no-leídos al abrir, Diana 1 no-leído); **traducir** renderiza `body_translated` + cachea (solo esa columna); **adjunto** upload PNG→confirm→chip→download (signed URL `scope=download`), `.txt` rechazado por allowlist, path con prefijo de conversación.
- **RLS pgTAP** (nuevos `12_messaging_participant_isolation.sql` + `13_messaging_realtime_conv_channel.sql`, README mapeado a DOC-31 §8.2 #21-22): participante ve conv/messages, no-participante/outsider 0; INSERT solo participante+sender=self+kind∈(text,attachment) [system/spoof→42501]; UPDATE/DELETE→0 filas (inmutable); admin override lee; predicado del canal `conv:{id}` (`is_conversation_participant`): participante ✓, outsider/deactivated/admin-no-participante denegados, deny-by-default, anon sin execute. _(Requieren `supabase test db` con stack local — corren en CI; no ejecutables vía MCP por `\set`/pgTAP.)_
- Gates (código final): **tsc 0 · eslint 0 · vitest 1256/1256** (+3 viewerCanPost) · **i18n 1520** (paridad es/en; vistas staff usan condicionales `tt(locale,…)` inline) · **build 0**.
- **Pendiente 7a**: OK de Henry + demo. **Diferido a 7b**: notificación anti-ráfaga de `message.sent` (hoy stub). _(Test data en seed conv `...-0a01`: 4 mensajes de prueba + 1 adjunto PNG 70B — removibles.)_

### Ola F6-3 — Contabilidad + Campañas de email + Emails react-email + Recordatorio manual ✅ (construido + two-stage review [3 BLOCKERs + 4 STRONG corregidos] + VERIFICADO EN VIVO)
SoT: DOC-55 §4-5 + PROMPT-AND-04/05 + DOC-44 §3.11 + DOC-47 Parte B §5 + DOC-26 §2.5 + DOC-73 §2-6 + DOC-48 (API-NOT-08..16 / API-BIL-11/12/15/16/18/19). **Cero migraciones** (schema verificado por MCP: `ledger_entries`/`broadcast_campaigns`/`campaign_recipients` + `client_profiles.marketing_opt_in` + `users.email_bounced_at` ya existían; FKs de los embeds PostgREST confirmadas). Decisiones Henry: adelantar Export CSV (P-55-2) + recordatorio manual (P-55-1); conciliación global (P-55-3) sigue diferida. Webhook Resend verificado sin túnel (test de firma svix + envío real).
- **Paso A — Fundación react-email**: instaladas `@react-email/components`/`@react-email/render`/`svix`; `src/backend/platform/emails/` (BrandLayout navy+gold + NotificationEmail genérica + CampaignEmail + render.tsx + i18n SUBJECTS). `deliver-notification` migrado de HTML hardcoded a render branded (todos los transaccionales con marca). Desviación: NotificationEmail genérica en vez de 1 tsx por templateKey (el pipeline guarda title/body i18n, no datos estructurados — branding es el deliverable de DOC-73 §3). Tests: 6 render (XSS escape, unsubscribe solo en campaña, fallback locale). vitest `oxc.jsx` configurado para `.tsx`.
- **Paso B — Contabilidad** (`/finanzas/contabilidad`): billing domain `validateLedgerEntry`/`monthRange`/`previousMonth`; repo `insertLedgerEntry`/`updateLedgerEntryRow`/`listLedger`(keyset)/`monthlyLedgerSummary`/`findCaseClientUserId`; service `recordLedgerEntry`(API-BIL-11)/`updateLedgerEntry`(12, **candado** payment_id!=null→NOT_EDITABLE)/`getMonthlySummary`(16)/`listLedger`(15)/`sendInstallmentReminder`(P-55-1, anti-spam 12h). Route **`GET /api/v1/billing/ledger/export`** (CSV síncrono, sin job/Storage — desviación justificada de DOC-26 §2.11; +anti CSV-injection). UI: selector mes, 3 KPIs+delta, desglose categoría, métricas cobranza (reusa getCollectionMetrics), libro con candado/Editar. Botón **"Recordar"** en kanban cobranza (reusa installmentId de overdueByCase/downpaymentByCase). +16 tests.
- **Paso C — Campañas** (`/finanzas/campanas` + `[id]`): módulo `campaigns` nuevo (domain máquina-de-estados + suppressionReason; repo resolveAudience[all/by_service/custom]+materialize+métricas+webhook helpers; service API-NOT-08..16 con `requireCampaignOrg`). Job **`send-campaign`** self-chaining (batches 100, dedupeId, cancelación mid-send, claim atómico scheduled→sending). Webhook **`/api/webhooks/resend`** (svix + idempotencia + bounce/complaint→supresión). Unsubscribe **`/api/unsubscribe`** (HMAC GET-confirm/POST-optout). UI: lista + editor (preview iframe sandbox, audiencia+conteo en vivo+supresión, métricas con bandas rebote/queja). +33 tests.
- **🛡️ Two-stage review** → **NEEDS-REVISION** (3 BLOCKER + 4 STRONG + 2 MED) → **corregido**: B1 HMAC unsubscribe reusaba clave PII → sub-clave derivada (domain separation); B2 CSV formula-injection → prefijo `'`; B3 `bodyHtml` de campaña sin sanear → `sanitize-html` allowlist al escribir; S1 GET unsubscribe lo disparaban scanners → GET confirma/POST ejecuta; S2 sendBatch fallido cerraba campaña con sent_count=0 → deja pending para retry; S3 dedupeKey de recordatorio perdía email en crash → siempre encola; S4 opt-out tardío no se suprimía → `suppressPendingRecipients`; MED-2 double-fire scheduled→sending → claim atómico `UPDATE WHERE status='scheduled'`.
- **VERIFICADO EN VIVO** (Andrium, Playwright + Supabase MCP, 0 errores de consola): **Contabilidad** carga con datos reales (KPIs $500/$0/$500 del cobro Ola-1; asiento automático 'cuota' con **candado "Generado por un pago — no editable"**; métricas cobranza coherentes con el KPI strip RF-AND-032) → **registrar gasto manual $150.50** → Egresos $0→$150.50, Balance $500→$349.50, desglose añade "marketing", fila Manual con botón Editar. **Campañas**: lista vacía → "Nueva campaña" crea borrador → editor con **preview iframe de marca** + **conteo audiencia en vivo "2 destinatarios · 3 suprimidos"** (resolveAudience + supresión). _(Test data: 1 gasto "Anuncios de prueba (smoke Ola-3)" $150.50 queda en los libros — removible.)_
- Gates: **tsc 0 · eslint 0 · vitest 1230/1230** (+55) · **i18n 1520** (paridad es/en; vistas Andrium usan condicionales es/en inline como pagos-global de Ola-2) · **build 0**.

### Pendiente menor / follow-up de F6-3
- Re-demo F1 completo (lead→firma→cobro real→activación) y envío real de campaña + webhook delivered/bounced en vivo → en la **demo con Henry** (necesita Stripe CLI + Resend live). La pieza F6 del re-demo (asiento automático + recaudado) ya verificada en la pantalla de contabilidad.
- `List-Unsubscribe`/`List-Unsubscribe-Post` header en el envío (one-click nativo Gmail/Outlook) → enhancement (el GET-confirm/POST ya mitiga el scanner). Plantillas react-email bespoke por templateKey (vs genérica) si se quiere layout de recibo rico → requiere enriquecer el payload de notificación.

### Ola F6-2 — Panel Andrium /finanzas (kanban cobranza + cola impresión + pagos/cuotas) ✅ (construido + revisado APPROVED×2 + VERIFICADO EN VIVO)
SoT: DOC-55 §1-3 + DOC-13 (RF-AND-001..027/044) + DOC-44 §3.7-3.12 + DOC-47 §3.8 + prompts andrium/01-03. **Cero migraciones.**
- **Backend cobranza** (billing/kanban/expediente, +~70 tests): `waiveInstallment`/`rescheduleInstallment`, `markOverdues`+`listReminderTargets`/`recordReminderSent`, `getCollectionMetrics`/`listDueCalendar`/`listOverdueForCollections`, job cron **`installment-reminders`** (registrado en JOB_REGISTRY); kanban `onInstallmentOverdue` (tarjeta Vencidas) + `onExpedientePrinted` (mueve a Hecho); expediente `listPrintQueue` + actions markPrinted/Shipped/Filed (API-EXP-18/19/20/21). Consumers cableados (service-role).
- **3 pantallas Andrium**: `/finanzas` kanban de cobranza (molde kanban Diana, board collections, KPI strip) · `/finanzas/pagos` (gate cuotas iniciales + tabs Calendario/Morosidad + estado de cuenta por caso con vías de cobro Stripe/Zelle/condonar/reprogramar) · `/finanzas/impresion` (cola, ciclo Impreso→Enviado→Radicado, visor PDF). i18n `staff.finanzas.*` (3 namespaces, paridad es/en).
- **🐛 3 bugs cazados por el smoke en vivo**: join inválido `cases→client_profiles` (500 en impresión → vía `case_members→users→client_profiles`); `t.raw` faltante (FORMATTING_ERROR); `client_profiles` objeto-vs-array (nombre mostraba UUID → "Carlos Ramírez"). Arreglados.
- **VERIFICADO EN VIVO** (Andrium, Playwright + Supabase MCP): 3 pantallas renderizan con datos reales (KPIs $500 recaudado/75% al día/$500 morosidad; calendario de vencimientos completo; estado de cuenta con invariante pagado+pendiente+vencido+condonado=total). **`markPrinted` → expediente `printed` + caso `delivered` + tarjeta kanban → "Hecho"** (cadena F5→F6). **Condonar cuota → `waived`** + motivo. **0 errores de consola.**
- **Two-stage review** → NEEDS-REVISION/**BLOCK-DEPLOY** → **APPROVED×2**: code-reviewer (2 BLOCKERs: `collectionMetrics` sin filtro org; `broadcastCardMoved` con card_id/columna/posición errados) + **security-auditor (CRITICAL cross-org IDOR)**. **Fix keystone**: `requireCaseAccess` ahora valida `org_id` para staff (cierra el gap en TODO el proyecto, no solo Ola-2 — era preexistente F2/F5) + helpers `requireInstallmentOrg`/`requirePaymentOrg` en las 5 mutations de billing. + fail-closed waive, requireSystemActor en cron, cota de reschedule. +15 tests negativos cross-org. **BLOCK-DEPLOY levantado.**
- Gates: **tsc 0 · eslint 0 · vitest 1173/1173** (+~88) · **i18n 1520** (paridad es/en) · **build 0**.

### Pendiente menor / follow-up de F6-2
- `listOverdueForCollections` filtra org solo en JS (correcto, no fuga; alinear a filtro DB como los otros en Ola-3). Constante `SYSTEM_USER_ID` hardcodeada en requireSystemActor (cosmético). Nombre del cliente: ya resuelto. Recordatorio manual / export CSV / conciliación global siguen diferidos (DOC-80).

---

## F6 — Billing + Andrium (por olas; cadencia: ola por ola con OK de Henry)

> Plan: `~/.claude/plans/analiza-mi-proyecto-y-twinkly-reddy.md`. Decisión Henry: Ola-1 = motor billing + Pagos cliente (la demo de DOC-80 §F6 centra el "cobro desde el celular"). Stripe **test real** (claves `sk_test`/`whsec` SOLO en `.env.local`, gitignored). **Cero migraciones de esquema** salvo 1 correctiva autorizada (índice anti doble-cobro). Olas: **Ola-1** motor+Pagos cliente · **Ola-2** panel Andrium (kanban cobranza, cola impresión, pagos/cuotas, morosidad, cron) · **Ola-3** contabilidad + campañas + emails react-email + re-demo F1.

### Ola F6-1 — Motor billing (Stripe + Zelle + ledger) + Pantalla Pagos del cliente ✅ (construido + revisado APPROVED×2 + COBRO REAL VERIFICADO EN VIVO)
SoT: DOC-44 (billing) + DOC-71 (Stripe) + DOC-51 §8/PROMPT-CLI-08 (Pagos cliente) + DOC-48 §3.5 (API-BIL-*). **Cero migraciones de esquema** (verificado por MCP: `installments`/`payments`/`ledger_entries`/`stripe_customers`/`payment_plans` completas; `ledger_entries` ya tenía índice único parcial `(payment_id,kind)`).
- **Backend `billing`** (extiende el slice F2; +97 tests): domain (`reanchorDueDates` regla "mensuales desde la firma", `isOverdue`/`daysLate`, máquina de estados con actor); service (`createCheckoutSessionForInstallment` [monto SIEMPRE de la BD], `handleStripeEvent`, `applyPaymentSuccess`+asiento ledger, `applyPaymentFailure`, `applyRefund`, `submitZelleProof`/`confirmZellePayment`/`rejectZelleProof`, `getAccountStatement`, `onContractSigned` reanclaje service-role); **webhook `/api/webhooks/stripe`** (firma raw-body `constructEvent`, idempotencia canónica `claimWebhookEvent`); actions API-BIL-01/03/04/05/06/07/08/13 + rutas Zelle/payment-status. Eventos `payment.proof_submitted`/`payment.refunded`.
- **Pantalla `/pagos` cliente** (PROMPT-CLI-08): tarjeta resumen navy (próxima cuota + monto + ProgressBar) + plan de cuotas con StatusPills + métodos Zelle (subir comprobante) / Tarjeta (Stripe Checkout). i18n `cliente.pagos.*`.
- **Two-stage review** → NEEDS-REVISION → **APPROVED×2**: code-reviewer (2 BLOCKERs: orden crash-safe del ledger en `applyPaymentSuccess`; doble-cobro TOCTOU → **migración 0019** índice único parcial `payments(installment_id) WHERE pending+stripe`, autorizada y aplicada) + security-auditor (3 HIGH: webhook reintento con `claimWebhookEvent`; IDOR fail-closed si case_id null; rate limits `billingCheckout`/`billingUploadUrl`; MED: orgId del ledger desde BD no metadata). **0 CRITICAL, sin BLOCK-DEPLOY.**
- **🐛 Bug i18n cazado por el smoke en vivo**: `dueDate`/`progressLabel`/`installmentRow` con FORMATTING_ERROR (templates con placeholders resueltos con `t()` en vez de `t.raw()`) — mismo patrón que /legal. Arreglado.
- **COBRO REAL VERIFICADO EN VIVO** (María, Playwright + Stripe CLI `stripe listen` + Supabase MCP): `/pagos` → "Pagar con tarjeta" → **Stripe Checkout real** ("Cuota 2 de 10 — ULP-2026-0001", $500 de la BD) → `4242…` → **webhook real [200]** → cuota 2 `paid`+`paid_at`, pago `succeeded`+`stripe_payment_intent_id`, **asiento `ledger_entries` income/'cuota' $500**, barra del cliente a **"2 de 11"** + próxima cuota = la 3. **Idempotencia**: 5 eventos Stripe → 1 pago, 1 asiento, 5 procesados, 0 firmas inválidas. **0 errores de consola.**
- Gates: **tsc 0 · eslint 0 · vitest 1112/1112** (+~95) · **i18n 1329** (paridad es/en) · **build 0**.

### Pendiente menor / follow-up de F6-1
- Si la API de Stripe hace timeout a mitad del checkout, el row de payment queda huérfano (bloqueo temporal, no doble cobro) → job de limpieza en Ola-2 (TODO en el código).
- Destino Zelle: hoy vía env `NEXT_PUBLIC_ZELLE_DESTINATION` (puente); config en admin settings más adelante.
- Diferido (fiel a DOC-80, "tras propuesta SoT"): recordatorio manual de pago, export CSV ledger, conciliación global → Ola-2/3.

---

> **F4 DoD CERRADA y commiteada** (`8ebb64c`): E2E automatizados (§4.3/§4.6) + QA visual + axe + test presupuesto (RNF-042) + RLS test 4 + seam de IA env-gated. Aclaración de numeración: en DOC-81 §4 los *flujos* E2E se numeran F1–F6 y NO coinciden con las *fases* (el "flujo F4" §4.4 es expediente/Abogados = fase F5).

## F5 — Diana + expediente + Abogados (por olas; cadencia: ola por ola con OK de Henry)

> Plan aprobado: `~/.claude/plans/analiza-mi-proyecto-y-twinkly-reddy.md`. **🔑 El SaaS Abogados REAL está disponible** (código `C:\Users\mauri\Documents\Trabajos\Abogados\Abogado`, Supabase propio `skmxmzwmrtjvslfccgan`, claves reales en su `.env.local`). Decisiones Henry: V2 se conecta al **SaaS real local (:3100)** para el loop E2E + **mejoras seguras del §10** al SaaS ahora (barrido completo después); alcance **core de producción legal** (mensajería/notificaciones → F7). **El modelo de datos de F5 ya existe** (migraciones 0008/0009): cero migraciones de esquema.

### Pre-ola — Migración 0018 ✅
- **0018 (`merge_form_answers` RPC) aplicada y verificada** al remoto. El autosave de formularios ahora usa merge atómico (sin race window).

### Ola F5-1 — Módulo expediente + ensamblador + carátulas ✅ (construido + revisado + VERIFICADO EN VIVO)
- **`platform/pdf`**: `renderCoverPdf` (carátula navy/gold determinista) + `compileExpedientePdf` (merge de N ítems PDF/imagen vía `graftPage` de mupdf + índice TOC two-pass con páginas de inicio). De-risk clave: `DocumentWriter` falla en forms USCIS ("substitute font creation") → `graftPage` lo resuelve; numeración de pie diferida (mupdf WASM no crea fuentes substitutas). Commit `7f92964`.
- **Módulo backend `expediente`** (domain/service/repository/events/index + **73 tests**): carátulas (`generateCover`), ensamblador (`createExpediente` con guard 1-draft + attempt_no, `getExpedienteMaterial`, add/remove/reorder/updateItem con FK lógica + estado editable, archivo externo), `compileExpediente` (descarga de buckets → compileExpedientePdf → sube a `expedientes` → compiled/page_count; compile_failed on error), `createCorrectionAttempt` (clona ítems, attempt_no+1, inmutable). **Review fixes:** quité 2 emits `compiled` erróneos (create/correction) + añadí el guard de prefijo `external/{caseId}/` (path-injection). Commit `d8cc6a1`.
- **UI ensamblador** `/legal/expediente/[caseId]` (server actions + page + `ensamblador-view`): crear expediente, generar carátula, agregar/ordenar/renombrar/TOC material, compilar, ver PDF. Commit `bf3d1d9`.
- **Verificado en vivo como Henry** (caso de asilo de Carlos): crear expediente → generar carátula real → agregar carátula + I-589 llenado → **Compilar** → el `compileExpediente` REAL descargó ambos de storage, los fusionó y subió al bucket `expedientes` → **PDF de 14 páginas válido** (TOC "Índice del expediente" + carátula + I-589 con Ramírez/Houston/Carlos). 0 errores de consola.
- Gates: tsc 0 · eslint 0/0 · **882 tests** (+73).

### Pendiente menor de F5-1
- Títulos de material genéricos ("Automated Form"/"Cover") — usar el label real del form/generación (pulido).
- Numeración de pie de página en el PDF compilado (mupdf WASM no soporta fuentes substitutas — requiere asset de fuente embebida).

### Ola F5-2 — Validación con abogado (V2 ↔ SaaS REAL) + mejoras al SaaS + UI Validaciones ✅ (construido + revisado + VERIFICADO EN VIVO contra el SaaS real). Commits V2 `bc714f2` · SaaS `03e07e1`.
SoT: DOC-70 (contrato, verbatim) + DOC-26 §2.8 (cron) + DOC-54 §6/PROMPT-DIA-06 (UI). **Cero migraciones** (esquema ya existe; verificado por MCP: `legal_validations` con `cancelled`, `ai_generation_runs.output_text`, `webhook_events`).
- **Módulo backend `integrations`** (domain/repository/service/events/index + **54 tests**, TDD): serializadores deterministas (`buildClientLabel`, `serializeAutomatedForm` §2.4.1, `buildAnnexIndex` — **PII jamás sale de V2**, solo texto); `sendToLawyer` (gates with_lawyer+compiled+sin-activa, paquete por `item_type`, `source` explícito, maneja 202/200-dedup/400/401/409/5xx); `applyVerdict` (único punto de efectos, idempotencia compartida webhook+polling vía `webhook_events` `'{validation_id}:{verdict_at}'`); `processVerdictWebhook` (HMAC-SHA256 sobre body crudo, `timingSafeEqual`, 401 firma ausente/inválida + fila forense, source guard); `reconcileFromPolling` (cron `retry-abogados-polling`, DOC-26 §2.8: 6h, sent_at<24h, escalación 72h). Route `/api/webhooks/abogados`. Consumers cableados.
- **UI Diana** (diana/06): `/legal/validaciones` (global, filtros) + `[caseId]` (loop del intento: stepper, semáforo 🟢🟠🔴+IA score, 7 StatusPill, cards de findings por severidad con "Atendido"+atajos, modal "Crear intento de corrección", **"Reenviar intento corregido"**, timeline, CTA "Enviar a Andrium" deshabilitado→Ola-3). Simulador local `scripts/simulate-abogados-webhook.mjs` (DOC-70 §9.2, casos negativos).
- **Mejoras seguras al SaaS** (repo `Abogados\Abogado`, §10, tsc/lint/build verdes): **§10.5 firma OBLIGATORIA** (no envía sin firmar), **§10.2 redelivery con retry/backoff** (3 intentos), **§10.7 409 con `validation_id`**. (§10.4 cancelled omitido: el SaaS no tiene flujo de cancelación.)
- **Two-stage review** (code-reviewer → 3 blockers): #1 consumers no cableados (arreglado), #2 cutoff 24h (intencional, DOC-26 §2.8 manda — DOC-70 §6 le delega), #3 forense silencioso (arreglado: siempre loggea). + HIGH #4 (quitados 3 imports dinámicos `as string`), #8 (polling recupera filas sin external_validation_id), nit limit.
- **🐛 Bug de PRODUCCIÓN cazado por el E2E en vivo**: `applyVerdict` corre en contexto webhook (**sin sesión**); `cases.changeCaseStatus`→`findCaseById` usa `createServerClient` (RLS) → `CASE_NOT_FOUND`. Fix: cambio de estado del caso vía **service-role** (`setCaseStatusSystem`, como expedientes) — también elimina el ruido de audit "system". +tests.
- **VERIFICADO EN VIVO contra el SaaS REAL local (:3100)** (caso de Carlos → `with_lawyer` por UPDATE de 1 fila autorizado): Diana **"Enviar a abogado"** → **POST real → 202** → `queued`+`external_validation_id`+expediente `sent_to_lawyer`+caso `in_validation` → webhook `needs_corrections` (HMAC válida) → `corrections_needed`+findings+semáforo+score (UI renderiza Zona 3 exacta) → **"Crear intento de corrección"** (modal→intento 2 clonado→ensamblador) → **recompilar** (14 pág) → **"Reenviar"** (200-dedup manejado) → webhook `validated` → expediente **`approved`** + caso **`ready_for_delivery`** (UI card verde "Listo para radicar"). **Seguridad**: firma corrupta→401, sin firma→401, source ajeno→200 no-op. **0 errores de consola.**
- Gates: **tsc 0 · eslint 0 · vitest 960/960** (+54) · **i18n 1247** (paridad es/en).

### Pendiente menor de F5-2 / hallazgos
- El loop **completo §5 "nueva validación al re-POST"** requiere que el SaaS REAL emita el veredicto (sesión de abogado); con el simulador el SaaS mantiene su validación activa → el reenvío recibe **200-dedup** (manejado correctamente). Para el §9.1 completo contra el SaaS real, configurar un abogado revisor en el SaaS.
- "Mi Historia" (`cliente/historia`) sigue siendo placeholder (hallazgo de F4).

### Ola F5-3 — Handoff a Andrium (impresión) + kanban de casos de Diana + E2E §4.4 ✅ (construido + revisado APPROVED + VERIFICADO EN VIVO). 🏁 Cierra F5.
SoT: DOC-45 §3.8–3.9 (handoff impresión, verbatim) + DOC-54 §0–1/PROMPT-DIA-01 (kanban Diana) + DOC-47 (motor kanban) + DOC-81 §4.4. **Decisión Henry: fiel a DOC-80 → la UI de impresión de Andrium (`/finanzas/impresion`) llega en F6; en F5 las actions quedan backend-ready.** **Cero migraciones** (columnas de impresión + `board_kind='cases'/'collections'` ya existían).
- **Backend `expediente`** (handoff, TDD): `sendToFinance` (gate `approved` with_lawyer | `compiled` self; bloqueo de reenvío; `requireCaseAccess`), `markPrinted` (RF-AND-025: exige `sent_to_finance`+`compiled_pdf_path`), `markShipped`/`markFiled` (exigen `printed`, sin cambio de estado). Eventos `expediente.sent_to_finance` + `expediente.printed`.
- **Consumers** (cableados en `register-consumers`): `kanban.onExpedienteSentToFinance` → tarjeta idempotente en board `collections` de Andrium, columna **"Por imprimir"**; `cases.onExpedienteSentToFinanceCase` → asegura `ready_for_delivery`; `cases.onExpedientePrintedCase` → `delivered`. **Lección de Ola-2 aplicada**: las transiciones de estado del caso en eventos usan **service-role** (`transitionCaseSystem`/`findCaseByCaseId`), NUNCA `findCaseById` (RLS sin sesión).
- **Kanban de casos de Diana** `/legal` (molde del board de leads de Vanessa): `getBoard{kind:'cases'}`, 5 columnas semilla, tarjeta de caso (icono+`ULP-…`+chip "Con abogado"+cliente+servicio·fase+chips de alerta+TimeBadge+nota), drag&drop optimista, gestión de columnas, i18n `staff.legal.kanban.*`. Reads nuevos en `cases`/`kanban`: `listCasesForParalegal`, `backfillCasesBoard`, `getCaseBoardAlerts` (agregado batch: por revisar / correcciones del abogado / generación fallida / RFE vencida). + `sendToFinanceAction` y botón **"Enviar a Andrium"** habilitado en `validaciones/[caseId]` (con estado idempotente "Enviado a impresión").
- **🐛 2 bugs cazados por el E2E en vivo** (no los veían los unit tests): (1) el seed del **motor kanban** (F3) reventaba al crear un board en runtime — `.upsert(onConflict:'board_id,position')` sobre una constraint **DEFERRABLE** (Postgres la rechaza como árbitro) → 0 columnas; **primera vez** que se crea un board fuera de un seed SQL. Fix: insert plano + swallow 23505 (`seedBoardColumns`) + **self-heal** en `getBoard`/backfill/consumer (si 0 columnas, re-siembra). (2) `backfillCasesBoard` colisionaba `unique(column_id,position)` (varias tarjetas a la misma posición) → posiciones incrementales en memoria. (3) i18n `FORMATTING_ERROR` por `t(key)` sobre templates con `{n}` → `t.raw()`.
- **VERIFICADO EN VIVO** (Diana, navegador MCP + Supabase MCP; caso de Carlos `with_lawyer` validado, reset de 1 fila autorizado por Henry): **"Enviar a Andrium"** → expediente `sent_to_finance` (`by`=Diana) + caso `ready_for_delivery` + **tarjeta automática idempotente** en board `collections`/"Por imprimir" de Andrium (1, sin duplicar) + badge "Enviado a impresión". **`/legal` kanban** renderiza 2 casos (María/Carlos) con alerta real "Correcciones del abogado" + 5 columnas auto-sanadas, **0 errores de consola**. `markPrinted→delivered` cubierto por los 51 unit tests del handoff + el mecanismo de consumer→transición service-role ya probado en vivo (gemelo `ready_for_delivery`).
- **Two-stage review** (code-reviewer → NEEDS-REVISION → **APPROVED**): 2 HIGH cerrados (`requireCaseAccess` en las 4 actions de impresión — gap de aislamiento por org) + STRONG (timestamp único en markShipped/Filed, `card_id` real en broadcast, typo es.json). `console.error`→`logger` descartado (boundaries prohíbe app→platform; va a stdout del server).
- Gates: **tsc 0 · eslint 0 · vitest 1043/1043** (+83) · **i18n 1291** (paridad es/en) · **build 0**.

### Pendiente menor de F5-3 / follow-up
- Perf (no bloqueante): N+1 en `listCasesForParalegal` (mismo patrón pre-existente de `listCasesAdmin`, escala paralegal ~5-20 casos); batch de `findCardByRef` en backfill; constante para la etiqueta "Por imprimir"; double-read en `onExpedientePrintedCase`.
- `markPrinted/Shipped/Filed` quedan **backend-ready sin UI** (la cola de impresión de Andrium `/finanzas/impresion` es F6, decisión DOC-80).

### Siguiente: F6 — Billing completo + Panel Andrium `/finanzas` (kanban cobranza + cola de impresión [ver/descargar PDF + Impreso→Enviado→Radicado] + pagos/cuotas + contabilidad + campañas). DOC-44/55/71/73.

---

## F4 — IA engine + formularios (por olas; cadencia: ola por ola con OK de Henry)

> Decisiones Henry: modelo de generación legal = **Sonnet 4.6 (default) + Opus 4.7 (premium por formulario)**, conservando Fable 5 / Haiku 4.5 para tareas ligeras. Plan: `~/.claude/plans/analiza-mi-proyecto-y-twinkly-reddy.md`.

### Ola F4-0 — Spike AcroForm (de-risk R1) ✅
**Riesgo #1 del proyecto RESUELTO.** Spike contra 3 PDFs gubernamentales reales (I-765/I-360/EOIR-26):
- **Hallazgo**: los formularios modernos de USCIS son **XFA híbridos** con object streams comprimidos → **pdf-lib los lee como 0 campos o crashea**. **mupdf (Artifex wasm, serverless-friendly) los lee completos**: I-765 = **161 widgets**, I-360 = **510 widgets** (name/type/page/rect).
- **Receta confiable de llenado+aplanado** probada (drop XFA → setTextValue → update → bake → valor **visible en el PDF aplanado**, round-trip confirmado).
- **mupdf también hace HTML→PDF** → un solo motor para llenado de forms + render de generaciones (md→html→pdf). DOCX vía lib `docx` (Ola 1).
- EOIR-26 público es print-and-sign (0 campos → ruta de error del editor).
- **Decisión de arquitectura**: `mupdf` es el motor de formularios PDF (desviación documentada del SoT que asumía pdf-lib; evidencia en `docs/_evidence/f4-spike/SPIKE-FINDINGS.md`). pdf-lib/@cantoo removidos.
- Gates: tsc 0 · eslint 0/0 · **525 tests**. Commit `3dd644b`.

### Ola F4-1 — Motor `ai-engine` (backend, TDD) ✅
Módulo `ai-engine` (domain 13 funcs puras, service API-AI-01..10, repository, events) + `platform/pdf.ts` (mupdf: render md→pdf/docx + detect/fill AcroForm) + 5 jobs (run-generation/extract-document/translate-document/ai-budget-aggregation/job-failed) + hook de extracción en `confirmDocumentUpload` + migración aditiva 0017. Whitelist sonnet-4-6 (default) + opus-4-7.
- **Two-stage review aplicado** (code-reviewer → 2 críticos + PII + evento roto + nits). El agente de fixes se estancó → **tomé los fixes directamente**: **C-1** barrier de idempotencia `webhook_events` en la ruta QStash (nuevo `platform/webhook-events.ts` + orgId en payloads; crons exentos); **C-2** guard cross-tenant en cancel/regenerate/retry; **B-3** `completeRun` ahora SÍ emite `generation.completed`; **H-4** `maskPii` recursivo; **H-5** traducción sin fuente → `failed`; **M-8** guard colisión `raw_text`; **L-13/14/15** + tests de regresión. Deferido **M-12** (structured outputs del editor T2) → Ola 2.
- Gates: tsc 0 · eslint 0/0 · **672 tests** · build ✓ (mupdf wasm). Commits `91ab66b` + `d8416bc`.
- **Pendiente externo (Henry, no bloquea Olas 2-3)**: aplicar migración 0017; cargar `ANTHROPIC_API_KEY` + `GEMINI_API_KEY` para los runs reales de la demo.

### Ola F4-2 — Editor de formularios admin ✅
- **Backend** (los 6 stubs `CATALOG_STUB_F4` completados): `createAutomationVersion` (PDF→`detectAcroFields` mupdf→versión draft), `redetectFields`, `aiProposeStructure` (T2→grupos+preguntas ES/EN), `generateTestPdf` (`fillAcroForm`), `proposeExtractionSchema` (T2, schema Gemini-portable), `testGeneration` (→`startGeneration` isTest); datasets CRUD + auto-conteo de tokens; M-12 (structured outputs/retry de T2).
- **UI**: editor `/admin/catalogo/[serviceId]/formularios/[formId]` (modo PDF 4-etapas con **visor pdfjs** + overlays de campos en 3 estados; modo ai_letter config|prueba, **modelo default `claude-sonnet-4-6`**), `/admin/datasets` (CRUD + banner anti-PII), `/admin/ai-costs` (KPIs + barra de presupuesto).
- **Verificación EN VIVO (Henry admin, navegador MCP)**: las 4 pantallas renderizan con datos reales, 0 errores. **2 bugs cazados+arreglados en vivo**: hydration mismatch (token locale en ai_letter) + crash RSC en `/admin/datasets` (arrow-action inline).
- **Two-stage review** (code-reviewer → NEEDS-REVISION): 3 HIGH de seguridad + 5 MEDIUM, TODOS corregidos: **path injection en uploads** (`createAutomationVersion`/datasets validan prefijo de bucket + `validateUploadedObject` magic-bytes), **`raw_text` guard en save-time**, **cross-tenant** en updateDataset/updateDatasetItem/testGeneration, guard de versión en deleteQuestion, orden IA-antes-de-borrar en aiProposeStructure, `listDatasets` con actor real.
- Gates: tsc 0 · eslint 0/0 · **727 tests** (+27) · build ✓. Commits `d931bab`+`19d4367`+`9dfe5c0`.
- **0017 aplicada al remoto** ✓ (vía MCP). **Claves IA cargadas + validadas** (ping real: sonnet-4-6 y gemini-2.5-flash responden).

### Ola F4-3 — Form-wizard runtime + cliente ✅ (construido + revisado + VERIFICADO EN VIVO)
- **Backend runtime** (`cases`, DOC-41 §3.8–3.10): `getFormForClient` (versión+grupos+preguntas+prellenado resuelto), `saveFormDraft` (congela `automation_version_id`, merge JSONB por clave), `submitFormResponse` (validación server-side requeridas+regex/min/max+**select whitelist**), `approveFormResponse` (gate `filled_by`), `generateFilledPdf` (`resolveBySource`→`fillAcroForm` mupdf→bucket `generated`, gates FORM_PDF_BLOCKED/FORM_VERSION_MISMATCH), `getCaseExtractions`. **PII de `profile` se descifra y resuelve LOCALMENTE, nunca a la IA.**
- **Form-wizard cliente** (motor compartido `frontend/features/form-wizard/`, SOT-3 DOC-50 §6): data-driven por grupos, **Zod generado**, **autosave** (debounce + cola IndexedDB + backoff), prellenado **"Ya lo tenemos"**, dictado por voz; pantallas `formulario/[formId]`, lista, **Mi Historia**. **El preview del editor admin (F4-2) ahora usa el MISMO motor** (TODO resuelto). Verificado visual (10 screenshots, 0 errores).
- **Two-stage review** (code-reviewer → NEEDS-REVISION): **2 CRÍTICOS cross-tenant** (approve/generate leían por responseId con service-client sin `requireCaseAccess` → un staff de otra org podía aprobar/generar el PDF con PII de un caso ajeno) + 3 HIGH (select whitelist, validación silenciosa con versión sin preguntas, merge no atómico) + nits — **TODOS corregidos** + tests de regresión cross-tenant. Migración aditiva **0018** (RPC `merge_form_answers` atómica) creada — **pendiente de aplicar** (código funciona vía fallback).
- Gates: tsc 0 · eslint 0/0 · **799 tests** (+72) · build ✓ · i18n 1182. Commits `b0fe2e5`+`2dccfec`+`50c90b6`.

### Ola F4-3 — VERIFICADO EN VIVO (2026-06-14, MCP Playwright) ✅ + IA de propuesta enriquecida
- **Prueba de fuego IA (generación)** ✅: config `ai_letter` (memorándum-asilo) guardada vía editor → generación real Claude `sonnet-4-6` → memo legal de asilo (INA §208), **$0.0329**, PDF 288KB (mupdf). Commit `632cf99`. Evidencia `docs/_evidence/f4-fire/`.
- **Editor pdf_automation con IA grounded** ✅ (pedido de Henry): subí el **I-589 oficial real** (835KB) → mupdf detectó **460 campos** → "Generar propuesta con IA" ahora **investiga en vivo (web_search nativo de Claude)** las instrucciones oficiales + procedimiento del servicio y genera **6 grupos / 51 preguntas coherentes ES+EN**, tipos correctos (Fecha/Desplegable/Checkbox/Número/Párrafo), selects con opciones (incl. los 5 fundamentos INA §208 + CAT), mapeo a `profile` (whitelist) con PII local. Publicado v1. Arquitectura **dos pasos** (research → JSON tool-free) para no truncar en formularios grandes. Commit `47496ac`. Evidencia `docs/_evidence/f4-editor/`.
- **Wizard del cliente recorrido como Carlos** ✅: render data-driven, prellenado **"Ya lo tenemos"** (Ramírez/Carlos/El Salvador/tel desde perfil), selects→radios ES, ayudas, **autosave que persiste en la BD** (verificado), contador "Paso 1 de 6", **0 errores de consola**.
- **4 bugs de PRODUCCIÓN cazados en vivo** (los unit tests los mockeaban) — todos arreglados + tests de regresión:
  1. `mergeFormAnswers` desligaba `supabase.rpc` de su cliente → `this` perdido → **TODO autosave fallaba** en prod. Fix `.bind(supabase)`.
  2. `saveFormDraft` exigía `required` sobre el patch parcial → borrador rechazado. Fix `validateAnswerTypes(..., enforceRequired=false)` para draft.
  3. `z.string().uuid()` (Zod v4 estricto RFC) rechazaba UUIDs válidos en Postgres (ids demo no-v4) en writes. Fix helper `zUuid` laxo (módulo cases).
  4. `resolveWizardLabels` formateaba la plantilla ICU `Paso {n} de {total}` → `FORMATTING_ERROR`. Fix: echo de placeholders.
- Gates: tsc 0 · eslint 0/0 · **806 tests** (+7) · build ✓.

### "Prueba de fuego" CERRADA end-to-end (2026-06-14) ✅
- **Cliente recorrió y ENVIÓ el wizard en vivo**: llené las preguntas del I-589 como Carlos (prefill + autosave) y **envié** (`draft→submitted` en la BD real). El recorrido completo del cliente queda probado.
- **PDF I-589 llenado** ✅: resolví las respuestas enviadas + datos de perfil (`resolveBySource`: client_answer + profile) y llené el AcroForm oficial con la receta mupdf de `platform/pdf` — **23/23 campos**, verificado por texto (Ramírez, Carlos, El Salvador, periodista, El Paso…). Script `docs/_evidence/f4-editor/fill-i589.mjs` (+ `i-589-LLENADO.pdf` gitignored). Commit `9beb44c`.
- **5º bug de prod (submit)**: `submitFormResponse` validaba requeridas contra el `answers` crudo, pero las preguntas `profile`/`extraction`/`generation` se resuelven al render/PDF (su valor NO está en `answers`) → un formulario con nombre/teléfono desde el perfil **nunca podía enviarse**. Fix: resolver fuentes no-cliente antes de validar requeridas (+2 tests). **808 tests verdes.**
- **Hallazgo de calidad IA** (no bug): la IA marcó varios checkboxes sí/no como `is_required` → el wizard los fuerza a "sí". El admin debería volverlos opcionales o `select` (Sí/No). Mejora de prompt para la próxima iteración.

### UI staff de aprobación/generación — CONSTRUIDA y verificada en vivo (2026-06-14) ✅
- **Pantalla `/admin/casos/[caseId]/formularios`** (RF-ADM-010 / DOC-53 §3.4.3): lista las respuestas del caso con su estado y permite **Aprobar** (submitted→approved) y **Generar PDF**. Read nuevo `getCaseFormResponsesForStaff` (responseId+status+label+party+filledBy+pdf) + componente `CaseFormsManager`. Enlace "Formularios →" en el detalle del caso. Commits `3e494df`+`6c5ed7d`.
- **Verificado en vivo como Henry**: Aprobar → `approved`; Generar PDF → el **`generateFilledPdf` REAL** produjo un **I-589 llenado válido (1.85MB, 12 págs, todos los datos incl. la dirección que el cliente sobreescribió)** en el bucket `generated`, URL firmada abierta, UI → Regenerar/Ver PDF.
- **Bugs #6 y #7 cazados al generar el PDF real**: (#6) `generateFilledPdf` ignoraba el override del cliente sobre un prefill `profile` vacío → ahora prefiere `answers`; (#7) **`uploadBytesToStorage` usaba `bytes.buffer`** (todo el backing store wasm) → **corrompía TODO archivo generado** (el 1er PDF salió 27.6MB ilegible) → copia exacta de la vista. +2 tests.
- **Calidad IA mejorada**: el prompt de propuesta ahora prohíbe checkboxes sí/no obligatorios (usar select Sí/No u opcional).

> **F4 COMPLETA**: motor IA + editor con IA grounded + wizard cliente (fill→autosave→submit) + runtime + **UI staff aprobar/generar PDF** — todo verificado en vivo. **7 bugs de producción** cazados por el E2E en vivo (todos arreglados+tests). **809 tests · tsc 0 · eslint 0.**

### F4 — Cierre de DoD (2026-06-15) — gates verdes
Cierre de los 4 entregables de DoD que faltaban (DOC-80 §F4 + §6). 100% infra de test, sin cambios de producto.
- **Seam de test de IA** (`platform/ai-stub.ts`): stub determinista env-gated (`AI_E2E_STUB=1`) para Anthropic (T1 stream/finalMessage, T2 create research/segmentación/schema) y Gemini (T3/T4 generateContent). **Guard anti-prod: LANZA si el flag está en `NODE_ENV=production`** (nunca IA falsa a clientes reales). Cableado en `anthropic.ts`/`gemini.ts`. Fiel a las formas que parsea `ai-engine` (verificado contra el código). **10 tests** (`__tests__/ai-stub.test.ts`).
- **Seam QStash** (`platform/qstash.ts`): bypass de firma SOLO si `isAiStubEnabled()` (imposible en prod) **y** header `x-e2e-qstash-bypass` — deja a Playwright ejecutar handlers de job por la ruta real sin reverse-engineering del JWT Upstash. Entregas reales de QStash en dev (sin header) siguen verificándose. Helper `e2e/helpers/qstash.ts`.
- **Test de presupuesto IA (RNF-042)**: `jobs/__tests__/ai-budget-aggregation.test.ts` — **12 tests** (umbrales 0.79→nada, 0.80→`over_80`, 1.0/1.5→`over_100`, monthly-close, idempotencia por `dedupeKey`, payload inválido).
- **RLS pgTAP "test 4"** (DOC-31 §8.2): `supabase/tests/rls/11_client_ai_pipeline_hidden.sql` — cliente miembro NO ve `ai_generation_runs` ni `document_extractions` (0 filas) + `throws_ok(42501)` al insertar + contraste positivo staff. 5 aserciones. (Ejecución real en CI con `supabase test db`.)
- **E2E Playwright de superficies F4** (render + datos reales + 0 errores de consola; auth setups Diana/Carlos nuevos): `f4-formulario.carlos` (runtime del wizard I-589), `f4-aprobacion.diana` (aprobación + **Regenerar PDF** real → asserta `filled_pdf_path` vía service_role), `f4-admin-surfaces.admin` (catálogo "Nuevo servicio"/prueba-de-fuego + editor pdf_automation de 460 campos + ai-costs + datasets). **Smoke 8/8 verde** (corridos con `--no-deps --workers=2`). Aserciones de BD vía `e2e/helpers/db.ts` (service_role, solo lectura).
- **QA visual + axe** (`e2e/visual/f4-*.visual.spec.ts`): captura light+dark de las pantallas F4 + axe-core (`@axe-core/playwright` instalado). **8/8 verde · 12 baselines generadas** (`e2e/visual/*-snapshots/`, light+dark × 6 pantallas). axe en **modo report-only** (gate con `A11Y_GATE=1`): aflora deuda **preexistente y global** de color-contrast del design system (bottom-nav `#94a2b8/#fefeff`≈2.56:1; chip verde `#1bb673/#e7f7ef`≈2.37:1) — NO es de F4; fix de tokens es transversal y con implicación de fidelidad al prototipo → **`<<NEED-A11Y-FIX>>`** (follow-up).
- **Verificación en vivo (MCP Playwright)**: login cliente (Carlos) y admin (Henry) OK; runtime del formulario cliente, editor de formularios admin (I-589, 460 campos detectados por mupdf), pantalla de aprobación staff, ai-costs, datasets, wizard "Nuevo servicio" — **todo renderiza con datos reales, 0 errores de consola**.
- **Two-stage review (REGLA #3)**: code-reviewer → **`<<APPROVED>>`** (los dos seams security-críticos son **estructuralmente imposibles de activar en prod**: `isAiStubEnabled()` lanza en `NODE_ENV=production` → propaga a `getAnthropicClient`/`getGeminiModels`/`verifyQStashSignature`; service_role key sin prefijo `NEXT_PUBLIC_` → nunca al bundle). Fixes aplicados: +2 tests de caller-path del guard de prod; edge-case rollover día-31 en `getPrevMonthUtc` (job + test). HIGH (passwords demo hardcoded) = convención existente (maria/vanessa, ya en `seeds/01`).
- **Gates: tsc 0 · eslint 0 · vitest 906/906** (+24: 12 presupuesto + 12 stub) · **check:i18n OK** (1182) · **E2E smoke 8/8 · visual 8/8 + baselines**.
- **Hallazgos registrados**: (1) "Mi Historia" (`cliente/historia`) renderiza un **placeholder "Tu historia, muy pronto"** — pantalla cliente del ai_letter aún stub. (2) El I-589 pdf_automation vive en fase `sustentos` (no la actual `reforzar` de Carlos) → la lista de formularios del cliente (phase-scoped) redirige a /historia. (3) Dev server **Turbopack** corrompe su manifest RSC bajo carga sostenida (`/login` 500) → `dev:e2e` cambiado a **Webpack** (estable). (4) Login staff con **rate-limit** persistente (Upstash) limita re-runs E2E seguidos.
- **Nota de alcance**: el *journey mutante completo* de DOC-81 §4.3 (fresh fill→submit→approve) y §4.6 (create→publish→activate) asume **BD efímera de CI** (DOC-81 §6); contra la BD demo persistente (formularios ya enviados/aprobados) los specs cubren el **render + las acciones no destructivas** (Regenerar PDF). El pipeline de generación IA queda probado de forma determinista por el stub (tests unit) + cobertura de render E2E.

### Pendiente menor de F4 (opcional)
- Aplicar migración **0018** (RPC `merge_form_answers` atómica) — opcional, el autosave ya funciona vía fallback (el `.bind` lo desbloqueó).
- Wiring de la pantalla como **tab "Formularios"** dentro de `shared-case` (hoy es página dedicada + enlace) — pulido menor.
- **Datos demo en prod** (vía editor, autorizado): I-589 v1 publicada + respuesta de Carlos aprobada + PDF generado. Útiles como demo.

---

— F3 (cerrada): —

**Fase F3 — Scheduling + Vanessa ✅ COMPLETA y verificada en vivo (3 olas + pendientes de datos cerrados)**

## F3 — Scheduling + Vanessa (por olas)

### Ola 1 — Backend `scheduling` + `kanban`/`leads` (TDD) ✅
- **`scheduling`** (DOC-43): `domain.ts` puro con `materializeSlots` (impl. literal DOC-23 §6.4: días civiles en la TZ de la regla, conversión por instante con date-fns-tz, DST gap/overlap, franjas cruzando medianoche, resta de excepciones/booked con buffer, min_notice/max_advance), `effectivePolicy` (override caso > política fase > default), penalizaciones, `validateRuleSet`, máquina de estados. `service.ts` API-SCH-01..15 (`getAvailableSlots`, `bookAppointment` con EXCLUDE gist anti-solape→SLOT_TAKEN, `cancel`/`reschedule` atómicos con penalización, `complete`/`noShow`, disponibilidad, `getWeekAgenda`). Eventos `appointment.booked/cancelled/rescheduled/completed`.
- **`kanban`+leads** (DOC-47): tableros lazy con columnas semilla exactas (§2.2 por kind), `moveCard` + broadcast Realtime `board:{id}`, leads (`createLead` con aviso duplicado 2-niveles, `markLeadWon/Lost`, `createCaseFromLead`→delega a cases), `expressServiceInterest` API-LEAD-08 (CTA público), `staff_tasks`. Listeners §3.8: `case.assigned`/`contract.signed`/`downpayment.confirmed`.
- **notifications** extendido (matriz F3: appointment.*, lead.created) + **job** `appointment-reminders` (QStash */15, idempotente por `reminder_1d/1h_sent_at`) + `jobs/registry.ts`.

### Ola 2 — Panel Vanessa: 8 vistas ✅
Réplica del prototipo `V2/UI Vanessa/` con los componentes desktop (DOC-52, prompts vanessa/01..08): **Mi día** (KPIs, por-atender-ahora con time-badge, agenda, tareas, embudo, Lex), **Leads** (kanban tarjeta §2.2 + DnD `moveCard`, modales Nuevo lead / Nuevo caso 2 pasos), **Citas** (CalendarGrid semana/día/lista, horas duales con etiqueta ET, SidePanel, modal Nueva cita Cliente/Prospecto), **Disponibilidad** (grid recurrencia, excepciones, settings, levantar bloqueo, migración TZ), **Clientes** (lista casos + shared-case reusado de F2), **Métricas** (embudo/barras/donuts SVG), **Configuración**, **LexDock**. Evidencia: `docs/_evidence/f3-vanessa/*.png`.

### Ola 3 — Cliente agendar/cita + E2E + review ✅ (construido)
- **Pantallas cliente** (`caso/[caseId]/agendar` + `cita/[appointmentId]`, DOC-51 §18-19, prompts cliente/18-19): calendario navegable, slots en **TZ dual** ("2:00 PM" / "12:00 PM en Utah" — reusa `frontend/lib/datetime.ts`), recordatorios 1d/1h, nota, CTA agendar (maneja SLOT_TAKEN→refetch, REBOOKING_BLOCKED→pantalla bloqueo con fecha de desbloqueo); detalle de cita con reagendar/cancelar, estado completada, botón videollamada (deshabilitado "Pronto" — LiveKit es F7), confetti. Actions `getSlots/book/cancel/reschedule`.
- **Two-stage review** (`code-reviewer`): NEEDS-REVISION → 10 hallazgos corregidos (TDD): **C-1** reschedule atómico (insert-primero: cero pérdida silenciosa, invariante documentado), **C-2** `expressServiceInterest` valida pertenencia a org + `no_phone` + limiter por IP, **H-3** orden de `moveCard`, **H-4** reminder mark-antes-de-encolar (anti doble email) + comentario, **H-5** logs PII-safe, **H-6** `LEAD_NOT_FOUND`, **M-7** `getWeekAgenda` DST-safe, **M-8** doc. Migración opcional `0016_scheduling_rpcs.sql` (RPCs atómicas) escrita pero NO aplicada — el reorder es la impl. activa.
- **BUG-LEADS-001** (encontrado por el E2E): `/ventas/leads` y `/ventas/mi-dia` (Server Components) crasheaban al llamar `sourceMeta()` de un módulo `"use client"`. Fix: helpers puros extraídos a `vanessa/shared/source-meta.ts` (sin "use client"); `ui.tsx` re-exporta. Build ✓.
- **E2E flujo F1** (DOC-81 §4.1): specs Playwright vanessa + maria (storageState; login email del cliente sembrado por sesión — el OTP en vivo espera SMTP). _(resultado en verificación — ver línea Gates F3.)_
- **EV-01/EV-02** (`service.published`/`form_version.published`): ya se emiten en `catalog/service.ts` — gap del plan ya cerrado.

| Gates F3 | typecheck **0** · lint **0/0** · **525 unit tests** · build ✓ · **1138 claves i18n** (paridad) · verificación en vivo MCP ✓ (8 vistas Vanessa + home/agendar/cita cliente, datos reales, 0 errores) |
|---|---|

### Verificación EN VIVO con navegador (MCP Playwright, 2026-06-13)
Dev server limpio + sesiones autenticadas reales (login real de Vanessa + sesión inyectada de María). **Las 8 vistas de Vanessa + home/agendar/cita del cliente renderizan con datos reales y 0 errores de consola.** Confirmado el feature núcleo: **hora dual** "10:00 AM (Florida ET) · 8:00 AM en Utah" en la cita real de María; calendario de agendar con días disponibles tappables + aviso de penalización 7 días.

**5 bugs reales encontrados SOLO por el dogfooding en vivo** (ninguno lo veía el build —páginas `force-dynamic`— ni los unit tests —no existía sesión cliente real antes del auth email—):
1. `ventas/mi-dia`: `onScheduleLead={() => {}}` pasado Server→Client (crash RSC, 500). Fix: prop opcional + default por router cliente.
2. `ventas/metricas`: `onPeriodChange={() => {}}` mismo crash RSC (500). Mismo patrón (default empuja `?period=`).
3. `cases.getCasesForClient`: llamaba `can()` (staff-only) → `AuthzError wrong_kind` en el `/home` del cliente (500). Fix: guard de kind cliente (RLS escopa filas) + **test de regresión**.
4-6. `cliente/home` i18n: `t()` sobre `phaseShort`/`greeting`/`documentsLeft` lanzaba FORMATTING_ERROR (mostraba las claves crudas) — son templates crudos sustituidos aguas abajo. Fix: `t.raw()`.

Evidencia: `docs/_evidence/f3-verify/verify.cjs` + screenshots MCP.

### Pendientes menores de datos — CERRADOS (2026-06-14, verificados en vivo)
1. **Nombre del asesor en la cita del cliente** ✅ — `scheduling.getAppointmentAdvisor` (requireCaseAccess + read service-role de solo `{displayName, avatarUrl}`). La cita muestra "Vanessa, tu asesora" + avatar. +6 tests.
2. **`getWeekAgenda` cableado a la vista Citas** ✅ — semana actual real (DST-safe) + cada cita enriquecida con `clientName` (batch client_profiles+leads, sin N+1). El bloque del calendario muestra "María", no el UUID. +5 tests.
3. **Agregados de Métricas reales** ✅ — `kanban.getSalesMetrics` (§6.2: embudo, fuentes, donuts, velocidad, asistencia); em-dash donde el dato es genuinamente desconocido (DOC-50 §5). +4 tests · +10 claves i18n.

Verificado en vivo (login real Vanessa + sesión de María acuñada con signInWithPassword contra el proyecto real): los 3 renderizan datos reales, 0 errores de consola. **Gates: tsc 0 · eslint 0/0 · 525 tests · i18n 1138 paridad.** Queda solo lo externo de Henry (SMTP para login email en vivo del cliente; migración 0016 opcional).

**Pendiente externo (no bloquea la construcción, paso de Henry)**: SMTP en Supabase Auth (Resend) para el login email en vivo del cliente; aplicar migración 0016 si se quiere la atomicidad por RPC (opcional).

---

## Parte A — Auth cliente por EMAIL (cambio al SoT DOC-22 §1) ✅
> Decisión del dueño: el cliente se autentica por **email** (capturado en el alta del caso), NO por OTP de teléfono. El teléfono queda como contacto opcional. Sustituto: **email OTP de 6 dígitos (Supabase nativo, cero Twilio)**.
- Backend refactorizado: `requestClientOtp`/`verifyClientOtp` por email, `provisionClientUser` idempotente por email (`email_confirm:true`, phone opcional), `checkClientEligibilityByEmail`, limiters email. domain `normalizeEmail`/`isValidEmail`/`normalizeEmailStrict`.
- Frontend: `/phone`→`/email`, `/otp` conserva (código por email), welcome CTA, middleware rutas públicas. Modal Nuevo caso: campo Email (identidad) + teléfono opcional.
- i18n `cliente.email.*` (796 claves, paridad). SoT DOC-22 §1 actualizado.
- Gates: typecheck 0 · lint 0/0 · **322 tests** · build OK · /email render verificado.
- **Pendiente externo (no bloquea F3, paso de Henry)**: configurar SMTP en Supabase Auth (Resend) — o un test-OTP en el dashboard — para que el código por email se entregue de verdad. Clientes demo María/Carlos ya tienen email; María tiene caso activo → lista para el demo de login email.

— F2 (cerrada): —

> **Gate del negocio F1 demostrado de punta a punta con navegador real (MCP):** staff crea caso (modal Nuevo caso →
> provisiona usuario cliente H-2 + caso payment_pending + contrato + plan) → cliente firma en /firma/[token] público
> (contrato signed, token anulado single-use) → staff registra pago Zelle → evento downpayment.confirmed → caso
> ACTIVADO (active + opened_at + fase 1 "Documentación inicial" + phase_history). Caso real: **ULP-2026-0003** (Rosa Méndez).
>
> **5 bugs reales encontrados SOLO por el dogfooding en vivo** (ningún test unitario los veía):
>   1. CRÍTICO: el event bus era un const de módulo → instrumentation y la server action tenían instancias
>      distintas (bundles separados de Next.js) → el consumer del gate NUNCA disparaba, el caso no se activaba.
>      Fix: bus respaldado en globalThis + flag de registro en globalThis.
>   2. createCaseFromContract NO existía (la action solo creaba un contrato huérfano) → implementado el orquestador
>      completo (DOC-41 §3.1) + provisionClientUser (H-2) en identity.
>   3. La activación no fijaba current_phase_id (getCatalogFirstPhase "diferido a F3" pero DOC-41 §3.4 lo pide en F2) → añadido.
>   4. El modal dejaba enviar una parte a medio llenar → error duro confuso. Fix: descartar filas de parte incompletas.
>   5. Cosmético: la firma pública mostraba "—" como servicio (plan_snapshot sin serviceLabel) → añadido al snapshot.
>
> Pendientes externos que NO bloquean F3: **Twilio Verify** (OTP SMS real — necesario para que el cliente entre por su
> celular; la demo del lado cliente quedó verificada a nivel de datos: Rosa es elegible por tener caso activo),
> Upstash Redis (rate limit prod), SMTP Resend en Supabase Auth, passwords reales del staff.

## Estado F2 por entregable

| Entregable | Estado |
|---|---|
| Módulos `cases` + `contracts` (máquinas de estado, createCaseFromContract idempotente, firma token, T&C, RFE, timeline) | ✅ |
| Slice `billing` (createPaymentPlan, registerZellePayment → downpayment.confirmed) | ✅ |
| `notifications` mínimo (dispatcher matriz F2) + job `deliver-notification` + webhook qstash + `instrumentation.ts` | ✅ |
| `provisionClientUser` (H-2: alta auth+users+client_profiles sin sesión, idempotente por phone) | ✅ |
| Componentes móviles DOC-01 §5.2 (ScreenHead, BottomNav, BottomSheet, Confetti, Tutorial, SignaturePad, FABs) | ✅ |
| Pantallas cliente (home, servicios, disclaimer+firma, camino, proceso, bitácora, documentos+subir+corregir+éxito, datos, más, config) | ✅ |
| Firma pública `/firma/[token]` (anti-enum, single-use, rate limit, scroll-gate, SignaturePad) | ✅ |
| Admin casos + shared-case (tabs Resumen/Documentos/Partes, RFE aprobar/rechazar, registro pago Zelle, modal Nuevo caso) | ✅ |
| pgTAP test 18 (signing token) | ✅ |
| Two-stage review F2 | ✅ NEEDS-REVISION → C-1/C-2/C-3/H-1/H-2/H-3/M-1..M-4 aplicados → gates verdes |
| Gates F2 | ✅ typecheck 0 · lint 0/0 · **316 unit tests** · build OK · 783 i18n · **E2E 30/30** · gate live verificado |
| Demo F2 a Henry | ✅ LISTA (gate del negocio funciona en vivo; ver ULP-2026-0003) |

— F1 (cerrada): —

## Estado F1 por entregable

| Entregable | Estado |
|---|---|
| Módulo `catalog` (CRUD servicios/planes/fases/hitos/docs, reglas publicación §2.4-2.6, getCaseRequirements, stubs F4) | ✅ |
| Módulo `audit` (writeAudit en toda mutación staff, listado, export CSV) | ✅ |
| `identity.inviteEmployee` + matriz permisos + EV-03 → email staff-invite | ✅ |
| Módulo `org` (nuevo: settings, terms_versions, cover_templates — decisión de cohesión documentada) | ✅ |
| Shell staff: Sidebar por permisos + 11 componentes desktop DOC-01 §5.3 | ✅ |
| Pantallas admin: dashboard, catálogo+wizard 6 pasos, empleados+matriz, auditoría+CSV, configuración | ✅ |
| pgTAP tests 6-9, 12-13 (37 aserciones) + corrección de fixtures F0 (bug de schema detectado) | ✅ / en curso |
| E2E Playwright wizard (17 tests — 16 esperan activación del Auth Hook; corren en CI local) | ✅ escrito |
| Two-stage review F1 | ✅ NEEDS-REVISION → 11 fixes aplicados + fail-closed extra → gates verdes |
| E2E Playwright (auth real, storageState) | ✅ 30/30 — evidencia en docs/_evidence/f1-final/ |
| Demo F1 a Henry | ✅ LISTA — login henry@usalatinoprime.com / changeme-henry! → /admin |

— F0 (cerrada salvo ítems externos): —

---

## Estado F0 por entregable

| Entregable | Estado |
|---|---|
| Repo + árbol DOC-21 + ESLint boundaries (5 reglas) + env Zod | ✅ |
| Migraciones 0001–0015 escritas Y aplicadas al remoto (69 tablas, RLS 100%) | ✅ |
| Seeds 01/02/03 aplicados (13 servicios, 4 staff, 2 clientes demo) | ✅ |
| Tipos generados `database.types.ts` + `check-db-drift.mjs` | ✅ |
| Suite pgTAP RLS (tests 1, 10–12, 14, 17 → 43 aserciones, corren en CI) | ✅ escrita (corre en CI con Docker; sin Docker local) |
| Design system DOC-01 (tokens, fonts, motion, 12 componentes brand, 54 iconos, Lex 0.977MB, showcase `/design`) | ✅ |
| Platform clients (15 archivos) + TDD (61 tests) | ✅ |
| Auth completa: OTP cliente con gate anti-enumeración, staff login/reset/cambiar-password, middleware guards, Actor/can() | ✅ código (demo E2E necesita credenciales ↓) |
| i18n next-intl sin prefijo + paridad es/en (73 claves) | ✅ |
| CI GitHub Actions (quality, rls-tests, db-drift, secret-scan) | ✅ escrito (sin repo GitHub remoto aún) |
| Two-stage review: code-reviewer → NEEDS-REVISION → fixes C-1/C-2/H-1/H-2/M-1/M-2/L-1/L-2 aplicados → gates re-verificados | ✅ |
| Gates: typecheck 0 err · lint 0 warn · 103/103 Vitest · build OK · boundaries-violation test probado y revertido | ✅ |

## Pendientes BLOQUEANTES para la demo F0 (necesitan a Henry)

> Actualizado 2026-06-12 (tarde): Henry entregó service role ✅, Resend ✅ y QStash ✅ — ya cargadas en `.env.local` y verificadas (QStash respondió 200 al event log; Resend es key send-only, válida pero solo verificable con un envío real autorizado).

1. **Ejecutar `supabase/fixes/2026-06-12-remote-repair.sql` en el SQL Editor** (una vez). Repara: (a) los 6 usuarios seed de `auth.users` que hoy hacen 500 "Database error querying schema" en TODA llamada de Auth (columnas token en NULL + teléfonos sin confirmar — bug encontrado por la suite Playwright); (b) el claim `must_change_pw` del hook (fix C-1 del review). Los seeds fuente ya están corregidos para futuros resets.
2. **Activar el Auth Hook** en el dashboard: Authentication → Hooks → Custom Access Token → `public.custom_access_token_hook`. Sin esto, ningún login (staff o cliente) pasa los guards (claims ausentes ⇒ unprovisioned).
3. **Twilio Verify** (Account SID, Auth Token, Verify Service SID) → configurar como SMS provider en Supabase Auth + habilitar proveedor Phone → OTP real por SMS.
4. **Upstash Redis** (REST URL + token) → `.env.local` (mientras tanto el rate limiting usa fallback in-memory SOLO en development). OJO: las credenciales QStash recibidas son del producto QStash (jobs); el Ratelimit necesita un Redis de Upstash, es otro recurso.
5. **SMTP custom de Resend en Supabase Auth** (dashboard) → emails de reset/invitación staff.
6. **Passwords reales de los 4 staff** (placeholders `changeme-*` del seed; resetear tras el fix 1).

## Decisiones tomadas (registro)

- **Repo en carpeta independiente** `C:\Users\mauri\Documents\Trabajos\usalatino-v2\` (decisión de Henry, 2026-06-12).
- **Sin Docker/CLI local**: migraciones aplicadas al remoto vía MCP; pgTAP corre en CI (ubuntu tiene Docker). `supabase db reset` local queda disponible si se instala Docker Desktop.
- **`src/middleware.ts`** en la raíz de src/ (Next.js lo exige; el árbol de DOC-21 lo dibuja dentro de `app/` — propuesta de corrección al SoT abajo).
- **Imports TS sin extensión `.js`** (Turbopack no resuelve `./x.js`→`x.ts`; typecheck/Vitest sí — gate de rutas 200 lo detectó).
- **Rate limiting**: tiers secuenciales (un deny corto no consume cuota de tiers largos — fix C-2); fallback in-memory SOLO en development con warning.
- **`must_change_pw` viaja como claim top-level del JWT** (fix C-1; getClaims() no expone app_metadata).
- **Logger escribe vía `console.*`, no `process.stdout`**: el middleware (Edge Runtime) lo importa y Turbopack rechaza process.stdout en bundles Edge. Mismo JSON estructurado en ambos runtimes.
- **invalid_phone devuelve `{ok:false}`** (M-3 del review): formato inválido es computable client-side, no filtra existencia de cuentas. Si Henry prefiere literalidad absoluta de DOC-22 §1.4 ("la UI transiciona SIEMPRE a OTP"), se cambia en 1 línea.
- **Upload TTL**: Supabase fija las signed upload URLs en 2h (DOC-27 §5 pedía 15 min — no configurable). Mitigación: token single-use + confirmación server-side.

## Propuestas de cambio al SoT (abiertas, esperan decisión de Henry)

| # | Doc | Propuesta |
|---|---|---|
| P-1 | DOC-21 §1 | Mover `middleware.ts` del dibujo `src/app/` a `src/` (requisito de Next.js) |
| P-2 | DOC-01 §2 | Plus Jakarta Sans solo existe hasta peso 800 en Google Fonts (el doc pide 900) — ratificar clamp a 800 |
| P-3 | DOC-01 §3 | `<<NEED-A11Y-FIX>>`: pares de contraste normativos (StatusPill/Chip texto-sobre-tinte: gold-deep/gold-soft 1.93:1, green/green-soft 2.37:1) no pasan WCAG AA — ¿ajustar tokens o aceptar con mitigación icono+texto (§8.4)? |
| P-4 | DOC-30 | La tabla interna `_case_number_counters` (helper de `next_case_number()`) no figura en las 68 — documentarla |
| P-5 | DOC-30 §11 | El bloque notifications dice "4 tablas" pero son 5 (`campaign_recipients`) — corregir conteo |
| P-6 | DOC-32 §4.1 | Seed 01: módulo `community` para finance sembrado como E (la matriz DOC-22 §6 lo confirma) — ratificar |

## Riesgos/recordatorios activos

- R2/F5: pedir **sandbox/staging del SaaS Abogados** con lead time (DOC-70 §10.3) — recordar al iniciar F3-F4.
- R1/F4: spike AcroForm en semana 1 de F4 con I-765/I-360/EOIR-26 reales.
- Advisors Supabase (WARN, no bloquean): extensiones pg_trgm/btree_gist en schema public; Leaked Password Protection desactivada (toggle en dashboard).
- Precios de `service_plans` en 0 (Henry los fija desde el editor Admin en F1+).

## Cómo retomar (sesión nueva)

1. Leer este archivo + `C:\Users\mauri\.claude\plans\analiza-mi-proyecto-y-twinkly-reddy.md` (plan aprobado).
2. Si la demo F0 ya tuvo OK de Henry → arrancar F1 (Catálogo + Admin core): leer DOC-40, DOC-53, DOC-14, DOC-22 §6, DOC-48 §3.1–3.2 y presentar plan corto de fase.
3. Si no → completar pendientes bloqueantes de arriba y hacer la demo (login OTP `+17865550101`, login staff, showcase `/design`).
